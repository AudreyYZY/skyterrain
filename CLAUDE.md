# CLAUDE.md

> Project instructions for Claude Code sessions.
> Read this file first before making any changes.

---

## 产品定位

**Flight Geography Explorer** — 飞机视角地貌认知系统

不是 GIS 仪表盘，不是飞行模拟器，不是游戏。

是一个让用户从飞机舷窗视角，认知地球地貌的纪录片式 Web 应用。

**两种模式**（顶栏切换，`localStorage` 记住 `fge-app-mode`）：
- **学习模式** `study` —— 地形地貌图鉴（默认；下文绝大部分内容都是这条链路，未改动）。
- **旅游出行模式** `travel` —— 城市 / 衣食住行 / 人文习俗 / 出行提示的概览。
  两套内容系统平行、互不影响：travel 只新增文件，study 链路原样保留。

## 范围

- **已有**: 亚洲（中国 79 + 日本 26 + 韩国 28 + 蒙古 26 + 泰国 25 + 越南 25 + 马来西亚 25 +
  新加坡 12 + 菲律宾 19 + 印度尼西亚 26 + 哈萨克斯坦 26 + 吉尔吉斯斯坦 25 + 乌兹别克斯坦 25 + 土耳其 28 + 斯里兰卡 25 + 文莱 12 = 432）+ 欧洲（英国 33 +
  冰岛 28 + 瑞士 27 + 挪威 28 + 法国 29 + 意大利 30 + 西班牙 26 + 德国 26 + 希腊 26 + 葡萄牙 26 +
  荷兰 24 + 奥地利 26 + 比利时 26 + 瑞典 26 + 芬兰 26 + 爱尔兰 28 + 丹麦 25 + 卢森堡 12 +
  波兰 25 + 克罗地亚 26 + 捷克 26 + 斯洛伐克 28 + 匈牙利 26 + 斯洛文尼亚 28 + 马耳他 12 + 爱沙尼亚 26 + 拉脱维亚 27 + 罗马尼亚 29 + 保加利亚 27 + 立陶宛 25 = 777）+ 北美洲（美国 26 + 加拿大 25 = 51）+
  南美洲（智利 28 = 28）+ 大洋洲（澳大利亚 34 + 新西兰 30 = 64），
  中英双语，共 1352 个（51 个国家）
  （`node --experimental-strip-types scripts/check-regions.ts` 每次运行都会打印准确计数，
  这份汇总数字如果又和它对不上，以脚本输出为准并回来改这一行）
  - 地形集选取标准与分类定义见 `docs/terrain-taxonomy.md`（T1 骨架 / T2 地貌省 / T3 标志地点；
    分类判据）
  - **`settlement`（绿洲·聚落）分类已废弃（2026-09-04）**：与真实地貌并排展示时显得违和，
    整体从地形注册表移除。中国 5 个条目（喀什/和田/吐鲁番/巴楚/麦盖提）迁移到旅游模式
    城市（见下方旅游模式进度）；荷兰 2 个条目（台丘聚落/羊角村）直接删除。详见
    `docs/terrain-taxonomy.md` §8 顶部的废弃说明。
- **进行中**: 全球主要地形（按大洲分组，逐国推进，每国 ≥25 个），见
  `docs/superpowers/specs/2026-08-30-world-terrain-expansion-design.md`
- **远期**: 全球地貌探索

**区域 = 大洲 → 次区域两级**（`lib/regions.ts`）。大洲 `REGIONS`：`asia` / `europe` / `africa` /
`north-america` / `south-america` / `oceania`；`DEFAULT_REGION_ID = "asia"`。次区域按联合国 M49
（`SUBREGIONS` 全量 + `COUNTRY_TO_SUBREGION` 每国一行 + `COUNTRIES` 显示名/目录顺序；
辅助 `countriesForContinent` / `subregionOfCountry` / `subregionsForContinent` / `getCountryMeta`）。
注册表条目 `regionId` 填大洲、`country` 填国家 slug（大洲由 `COUNTRY_TO_SUBREGION` 派生，必须一致）。
- 顶栏 `RegionSelector` = 大洲下拉，多次区域的大洲展开二级（点次区域→飞该次区域地形重心，
  重心由 `lib/subregion-geo.ts` 从注册表算）。
- 左侧 `IndexRail` 目录按**当前大洲下的国家**分栏（不再按分类/添加顺序）：国家序 = 次区域地理序 →
  `COUNTRIES` 序；国家内地形序 = T1→T2→T3（`lib/terrain-tier.ts`）再骨架类地貌优先再注册表序；
  次区域名作分隔小标题。
- `ContinentIntro`（学习模式首屏）= shuffle 的大陆滑动卡片，滑到哪片地球飞哪片，「开始探索」进入；
  `localStorage fge-intro-seen` 记住后跳过。旅游模式仍用 `IntroOverlay`。
- 地图任意点击：`CesiumMap` LEFT_CLICK→`drillPick`→最小面积地形→`onTerrainSelect`；
  `ExplorerApp.handleMapTerrainSelect` 自动切到该地形所在大洲再讲解（跨洲可用）。
- `localStorage fge-active-region` 读时把旧值 `china`/`xinjiang`/`australia` 迁移到大洲。
自检：`node --experimental-strip-types scripts/check-regions.ts`（regionId/country 完整性 +
terrainCount 核对 + 次区域↔大洲↔地形一致性）。

## 技术栈

- Next.js 16 (App Router)
- React 19
- TypeScript 5
- CesiumJS 1.141 (3D 地球)
- TailwindCSS 4
- Edge TTS (edge-tts-universal)

## 核心架构

### 数据模型

```
TerrainEntity          — 地形实体（天山、秦岭、四川盆地等）
GeometryRecord         — 几何记录（Polygon, LineString, RidgeCorridor）
FeatureOfInterest (FOI) — 讲解兴趣点（太白山、成都、格尔木等）
Story                  — 故事内容（seeing, formation, history, observation）
KnowledgeLink          — 知识关联（地形 ↔ 知识点）
ProvenanceRecord       — 数据溯源（来源、精度、获取时间）
```

### 关键文件

```
components/
  ExplorerApp.tsx        — 主编排器（状态 + 讲解/航线/语音链路 + 组合下面几个 UI）
  CesiumMap.tsx          — 3D 地球、Camera 系统、地形区域抬升高亮（hover/选中）、INTRO_VIEW 初始构图
  CesiumOverlayLabels.tsx — HTML 地形标签层（zoom 自适应、hover/focus 高亮）
  IntroOverlay.tsx       — 旅游模式初始标题卡（localStorage 记住已看过）
  ContinentIntro.tsx     — 学习模式初始：shuffle 的大陆滑动卡片，选定即飞入该大洲
  IndexRail.tsx          — 左侧地貌目录（窄条 → 浮出「次区域 → 国家 → 地形」，divider 分隔次区域）
  ReadingPanel.tsx       — 右侧单一阅读面板（卡片态 ⇄ 文章态，逐句高亮）
  JourneyBar.tsx         — 底部航线胶片条
  StructuredLesson.tsx   — 板块讲解渲染（editorial 衬线排版）；lesson=6 板块 / sections=通用段列表
  ModeToggle.tsx         — 顶栏 学习 / 旅游 切换
  CityMarkers.tsx        — 【旅游模式】地图上的城市点（轮询相机 zoom，按 tier 分级显示）
  TravelPoiMarkers.tsx   — 【旅游模式】选中城市时标注机场 + 攻略提到的地标

lib/
  terrain-registry.ts    — 【单一真实源】地形的位置/锚点/范围/走向/中英名/regionId(大洲)/country（选取标准见 docs/terrain-taxonomy.md）
  regions.ts             — 大洲配置 + 次区域(M49) SUBREGIONS/COUNTRY_TO_SUBREGION/COUNTRIES + 辅助函数 + DEFAULT_REGION_ID
  subregion-geo.ts       — 每个次区域的地形数量与地理重心（由 terrain-registry 派生，供顶栏二级下拉飞行）
  terrain-tier.ts        — terrainTier(id) → T1/T2/T3（由标签重要性派生）+ categoryOrder（目录排序用）
  terrain-camera.ts      — 数据驱动相机推导 computeTerrainCamera()
  terrain-content.{zh,en}.ts — 6 板块讲解的合并导出（正文在 lib/content/<country>/terrain.{zh,en}.ts）；terrain-content.ts = 界面按国家懒加载
  terrain-lesson.ts      — resolveLesson(id, lang)：一处决定用哪份讲解（内容→stories→兜底）
  terrain-label-registry.ts — 标签（由 terrain-registry 生成，含 nameEn）
  terrain-label-theme.ts — 标签视觉 token；LABEL_FONT_FAMILY = 通用系统字体栈
  lesson.ts              — 板块顺序 / 标题（中英）/ 拼接
  routes.ts              — 航线（data/routes/*.json，当前 295 条；航班号仅在有核实留痕时对外显示，见「航线的航班信息」一节），机场航点 + 地形航点
  route-narration.ts     — 每条航线两套 ~2.5 分钟连贯解说：ROUTE_NARRATION[id].{study,travel}（中英）；
                           getRouteNarration(id, lang, mode)；CesiumMap.flyRoute 播放
  app-mode.ts            — AppMode 类型 + getStoredMode/setStoredMode（localStorage fge-app-mode）
  places-registry.ts     — 【旅游模式单一真实源】城市 CityEntry（经纬度/tier/机场）+ 国家概览
  travel-content.{zh,en}.ts — 【旅游模式】攻略合并导出（正文在 lib/content/<country>/travel.{zh,en}.ts）
  content/<country>/     — **内容按国家分目录**（2026-09-14）：registry / terrain.zh / terrain.en / cities / travel.zh / travel.en / pois / routes，
                           合并索引由 npm run gen:content 生成到 content/_generated/，check:content 守着（见下方「内容目录」）
  travel-lesson.ts       — TravelGuide 类型 + resolveTravelGuide(id, lang) + travelGuideToSections
  travel-rail.ts         — 旅游模式左侧目录（当前大洲下按国家分组）
  travel-speak.ts        — 旅游攻略分段合成 + 预取播报编排（首段先出声）
  travel-pois.ts         — 每城市攻略提到的地标概略坐标（POIS_BY_CITY）
  i18n.ts                — UI 国际化；getTerrainName 查注册表
  i18n-stories.ts        — 早期 6 个双语故事（resolveLesson 的次级来源）
  terrain.ts             — 早期地形 JSON 数据的注册入口（坐标由 terrain-registry 覆盖）
  speech.ts              — TTS 系统（Edge TTS + 浏览器回退，跟随 language）

features/
  china-core-features.ts       — 早期核心地形定义（GeographicFeature）
  china-northwest-features.ts  — 早期核心地形定义（中国西北：天山 / 昆仑 / 阿尔泰 等）
  types.ts                     — GeographicFeature 类型
```

## 当前阶段

**地图/交互/双语已完成**：地形注册表（见 §范围，当前 1352 个）+ 数据驱动相机 +
标签分级（双语）+ 地形抬升高亮 + 自然语音（跟随语言）+ 逐句高亮 + 真实航班航线 +
纪录片编辑式界面。

**多国扩展进行中**：新增一个国家的地形（学习模式）=
⓪ `lib/content/countries.ts` 末尾加国家 slug、建 `lib/content/<country>/`，写完跑 `npm run gen:content`
① `lib/content/<country>/registry.ts` 加条目（`regionId` 填大洲、`country` 填国家 slug）
②该大洲若此前 `available:false` → 改 true；`terrainCount` 由 `check-regions.ts` 核对
③`lib/content/<country>/terrain.{zh,en}.ts` 写双语 6 板块 ④`lib/terrain-label-registry.ts`
`IMPORTANCE_BY_ID` 只对特别重要的补（默认已是 regional）
⑤`check-terrain-camera.ts` + `check-regions.ts` 通过。

**权威文字内容**：讲解改为 6 个通用板块（`TerrainLesson`，顺序见 `lib/lesson.ts`
`LESSON_SECTION_ORDER`）：
`seeing 概述` → `formation 地貌特征` → `observation 从空中怎么看` →
`distinguish 与相似地形的区分` → `concept 地理知识（为何算这类地形/常见误区）` →
`history 历史与人文`。
内容写在 `lib/content/<country>/terrain.{zh,en}.ts`（`getTerrainContent(id, lang)` 按注册表 id 找到国家再懒加载该国一份），
依据中国国家地理 / 中科院 / 自然资源部 / Geoscience Australia / Parks Australia / UNESCO
等公认地理事实总结，非文学化旁白、非凭空生成。
**已逐句核源（Block D，2026-08）**：中国 39 + 澳大利亚 22 = 61 篇双语。
核源标准：去比较性/主观评价，有争议的加限定或并列，查不到宁可删，数字统一到权威口径。
**占位地形已全部补齐（2026-08-29）**：中国其余的次级山脉、丘陵、湖泊、河流、草原、沙漠等
也写了完整 6 板块双语 —— 至此所有地形 100% 有讲解，不再出现占位文案。
内容优先级：`getTerrainContent(id)`（zh-CN）> `i18n-stories` 英译 > 早期地形 JSON `lesson` /
`core-features` story > 占位。

## 双模式（学习 / 旅游）

**Approach A：顶层模式开关 + 平行内容系统。** `ExplorerApp` 持 `mode: AppMode`，
分支只在 ~5 个渲染点；travel 全部是新文件，study 链路一行没动。

- **状态**：`mode` / `activeRegion` 首帧用 SSR 默认（`study` / `china`），
  挂载后 `useEffect` 从 `localStorage` 恢复（避免 hydration mismatch）。
- **学习模式**：`CesiumOverlayLabels` 地形标签 + `railGroups`（14 分类）+ `ReadingPanel`
  接 `resolveLesson` 6 板块 + `JourneyBar` 航线。地图 hover 拾取地形（`modeRef` 门控，
  travel 时禁用并清掉高亮）。
- **旅游模式**：`CityMarkers` 城市点（`places-registry`）+ `travelRailGroups`（当前大洲下按国家分组）+
  `ReadingPanel` 接 `travelGuideToSections`（通用段列表，走 `StructuredLesson` 的 `sections` 分支）。
  点城市 → `CesiumMap.focusCity(lon, lat)` 飞过去 **+ 自动开始播报**；点国家概览 → 飞大洲中心。
- **旅游播报（`lib/travel-speak.ts`）**：分段合成 + 预取管线（播第 i 段时合成第 i+1 段），
  首段先出声，不等整篇。`useSentenceHighlight` 逐句高亮（`startHighlightWithTiming` 带 `baseIndex`，
  无 word boundary 时 `startHighlightChunkEstimated` 估时）。按钮有"准备语音…"态（`ReadingPanel.isPreparing`）。
- **旅游 POI 标注**：`lib/content/<country>/pois.ts`（每城市攻略提到的地标概略坐标，`lib/travel-pois.ts` 合并导出）+ `TravelPoiMarkers`：
  选中城市时在地图上标机场（✈）+ 地标。只是静态标注，不做与播报同步的高亮（文本↔坐标匹配不可靠）。
  **之后每个国家的旅游模式都要保持这套行为一致。**
- **航线**：一条航线两套解说，跟随当前 `mode`（`getRouteNarration(id, lang, mode)`）；
  某模式解说为空时该模式下航线不播。当前 295 条航线 study + travel 解说均已写。
  **航班号/机型现在必须有 `source` 核实留痕才对外显示**，见下方「航线的航班信息」一节；
  没有留痕的航线照常可飞、地理解说照常播，只是界面与搜索都不出现航班号/机型。
  **中国国际航线（Phase 3，已完成，2026-09-04）**：中日既有 3 条之外，分 7 批（A–F2，
  F 批因研究量较大拆分 F1/F2 并行）新增 32 条中国—他国真实直飞航线，覆盖已收录的 35 个
  国家中的 32 个（每国落地该国已收录的 Tier 1 首都/首府或次一级已收城市）；Luxembourg、
  Iceland、Croatia 3 国查无真正不经停的客运直飞（分别是纯货运航司、经停哥本哈根的第五
  航权航线、经停布加勒斯特的同机号航线），如实标注缺口未收录，不编造经停航班冒充直飞。
  完成标准与分批记录见 `docs/expansion-playbook.md` §6.3。
- **新增国家两个模式都要做**：study = 地形注册表 + 6 板块讲解；travel = `places-registry`
  加城市（`lib/content/<country>/cities.ts`）+ `COUNTRY_TO_CONTINENT` 补映射 + `lib/content/<country>/travel.{zh,en}.ts` 写 7 段 TravelGuide +
  `COUNTRY_OVERVIEWS` 加概览 + `travel-pois.ts` 补该城市攻略提到的地标坐标。
  自检 `node --experimental-strip-types scripts/check-places.ts`（城市坐标/IATA/来源/双语内容齐全）。
- **进度**：
  - 澳大利亚旅游模式：1 国家概览 + 8 城市（悉尼/墨尔本/布里斯班/珀斯/阿德莱德/凯恩斯/达尔文/
    吉朗）；2026-09-04 补齐维多利亚州第二城市吉朗（大洋路门户，呼应"十二门徒岩"地形），
    同批修正达尔文 tier 误标（notable→major），见 `docs/expansion-playbook.md` §6.2——
    澳大利亚 8 个州/领地 Tier 2 骨架（每州≥2城市，ACT 单一城市豁免）至此收尾。
  - 中国旅游模式：1 国家概览 + 73 城市，34 个省级行政区 Tier 1（首府/主要城市）已全部收全
    （2026-09-02 补齐福州/济南/长沙/南宁/海口/兰州 6 个缺口，见 `docs/expansion-playbook.md` §6）；
    Tier 2 批1（2026-09-02）新增大连/宁波/苏州/洛阳/大同/黄山/泉州/张掖 8 城，同批 6 条国内航线
    （北京-大连、广州-宁波、上海-大同、上海-洛阳、西安-张掖、北京-黄山）；
    Tier 2 批2（2026-09-03）新增温州/扬州/珠海/承德/武夷山/景德镇/景洪/无锡 8 城，同批 6 条
    国内航线（昆明-景洪、北京-珠海、上海-景德镇、广州-温州、北京-扬州、北京-武夷山）；
    承德因紧邻北京未找到有意义的商业直飞航线，如实未收录；无锡与已收录的苏州共用同一机场
    （苏南硕放国际机场）；2026-09-04 新增南疆绿洲聚落 5 城（喀什/和田/吐鲁番/巴楚/麦盖提，
    原地形注册表 `settlement` 分类废弃后迁移而来）——喀什、和田均有真实商用机场并已有
    `urc-khg`/`urc-htn` 国内航线；吐鲁番交河机场（TLQ）实际在正常运营，但未找到可核实的
    固定航线，如实未收录航线；巴楚、麦盖提均无民用机场，如实按公路/铁路描述，不编造航线；
    Tier 2 批4（2026-09-04，见 `docs/expansion-playbook.md` §6.1）——审计发现内蒙古/吉林/
    黑龙江/湖北/四川/贵州/陕西/青海/宁夏 9 个省级行政区只收了首府、没有第二座城市，逐一补
    呼伦贝尔/吉林市/漠河/宜昌/乐山/凯里/延安/格尔木/中卫，**中国 Tier 2 骨架至此收尾**；
    同批新增 4 条国内航线（北京-呼伦贝尔、北京-宜昌、北京-延安、成都-格尔木）；吉林市、
    乐山均无在运营民用机场，如实未收录；漠河/凯里/中卫的机场航线现有资料均只能核实到
    经停枢纽中转、非不经停直飞，如实未收录航线。
    之后按热度继续扩 Tier 2/3。
    **Tier 2 批5（2026-09-05，跨 session 长任务开始，见 `docs/expansion-playbook.md` §6.4 与
    `docs/city-expansion-progress-2026-09-05.md`）**：新增 16 座省内重点城市（大理/香格里拉/
    开封/平遥/曲阜/泰安/烟台/秦皇岛/延吉/林芝/遵义/汕头/潮州/绍兴/北海/嘉峪关，中国旅游
    模式城市 73 → 89），并把北京/上海/天津/重庆/香港/澳门/台北 7 个条目的 layout 段补成
    按区（堂区）详述——不拆成独立地点。无机场城市如实不写机场字段（开封/平遥/曲阜/
    泰安/绍兴）；本批未加航线。
    **同日续做**：批6/7/8 又加 33 座中国城市（中国旅游城市 → 122），澳大利亚批2 加 15 城
    （18 → 33，8 个州/领地各 ≥2 城，按 ABS 主要城区口径），详见 playbook §6.4/§6.4.1 与
    进度文档。后续：中国批9 / 澳大利亚批3 候选已列在进度文档 §3，然后转其他国家。
  - **城市介绍深度重构（2026-09-06 起，优先于继续加城）**：`TravelGuide` 新增可选第 7 段
    `howItWorks`「这座城怎么运转」（渲染在概览之后），固定回答 5 个问题：谁在管 / 中心在哪 /
    本地人的地址语言 / 生活半径由什么决定 / 外来者最常见的结构性误解。全局视角、只写可核验的
    制度与结构事实、不用个人经历。规则与来源记录见 `docs/city-depth-redesign-2026-09-06.md`；
    试点 5 城（悉尼/上海/墨尔本/东京/伦敦）用户审阅后定稿为「纪录片旁白」语体（设计文档 §1 第三版）；
    **全部 691 座城市已于 2026-09-06 写完**——Tier A（capital + major）228 座分 12 批、
    Tier B（notable）463 座分 13 批，中英各自成文，每城带 `// howItWorks sources:` 注释，
    共 25 个内容 commit 在 PR #136 分支上，铺开记录见设计文档 §3.1/§3.2。
    Tier B 篇幅与 Tier A 取齐（中文约 280–330 字），设计文档原定的「≤120 字」已作废。
    **此后新增任何城市都必须同时写 `howItWorks`**，否则新条目会比既有条目少一段。
  - 中国政策类内容口径：签证/免签天数不写死，只说"近年放宽、以国家移民管理局最新公布为准"；
    进藏航线务必写"外国游客需西藏旅行许可 + 随有资质旅行社结伴出行"。
  设计与计划见 `docs/superpowers/specs/2026-08-29-two-mode-travel-study-design.md`、
  `docs/superpowers/plans/2026-08-29-two-mode-phase-1.md`。

## 全球地形扩展（学习模式，进行中）

设计见 `docs/superpowers/specs/2026-08-30-world-terrain-expansion-design.md`。

- **Phase A（已完成）**：区域重构为大洲。中国并入亚洲、澳大利亚并入大洋洲，顶栏改下拉，
  行为与之前等价。
- **Phase B 逐国加地形，每国 ≥25 个 T1/T2/T3**：
  - ✅ 美国 26 · ✅ 加拿大 25（`north-america` 共 51）· ✅ 日本 26（`asia` 84→110）
    · ✅ 新西兰 30（`oceania` 首批 22→52，之后澳大利亚自身又补 12 条 → oceania 现为 64）
    · ✅ 英国 33（`europe` 0→33，首个欧洲国家）
    · ✅ 冰岛 28（`europe` 33→61）· ✅ 瑞士 27（`europe` 61→88）· ✅ 挪威 28（`europe` 88→116）
    · ✅ 法国 29（`europe` 116→145）· ✅ 意大利 30（`europe` 145→175，首个南欧国家）
    · ✅ 西班牙 26（`europe` 175→201）· ✅ 德国 26（`europe` 201→227）
    · ✅ 希腊 26（`europe` 227→253，PR #65）
    · 之后陆续合并：葡萄牙 / 荷兰 / 奥地利 / 比利时 / 瑞典 / 芬兰 / 爱尔兰 / 丹麦 / 卢森堡 /
      波兰（`europe` 现为 499）· 泰国 / 越南 / 韩国 / 蒙古 / 马来西亚 / 新加坡 / 菲律宾 /
      印度尼西亚（东南亚全覆盖）
    · ✅ 哈萨克斯坦 26（`asia` 300→326，开启中亚次区域 `central-asia`；旅游模式 1 概览 + 12 城）
      **阿特劳 / 塔尔迪库尔干（2026-09-16）**各开一个新一级单位（阿特劳州 / 热提苏州）。本国的 howItWorks 主线是
      **「市」在行政上常常是一圈乡村加一座城** —— 阿特劳城区 32.9 万、市行政区 43.1 万，差的 10.3 万在统计上算农村人口；
      塔尔迪库尔干 17.2 万 / 21.0 万同理，而阿斯塔纳 / 希姆肯特 / 卡拉干达一个农村人口都没有（统计局同表可查）。
      塔尔迪库尔干另一条：**一步没挪却当过三个州的州府**（塔尔迪库尔干州 → 1997 撤销并入阿拉木图州 → 2001 起阿拉木图州府 →
      2022-06 新设热提苏州、州府定在本市，阿拉木图州府同日迁科纳耶夫）。
      ⚠️ **阿基姆的产生办法 2023–2026 年间反复变动，按发布门槛 ④ 一律不写**；统计局人口表的下载地址**必须带 `/file/ru/` 后缀**，不带会 404。
      本国惯例句 2026-09-16 改为「按贸易与一体化部的说法，餐厅的服务费必须事先写在菜单上，菜单上没写就不能收」（10 条），**继续不写百分比**。
    · ✅ 土耳其 28（`asia` 326→354，开启西亚次区域 `western-asia`）
    · ✅ 克罗地亚 26（`europe` 499→525，南欧第五国）
    · ✅ 捷克 26（`europe` 525→551，东欧第二国）
    · ✅ 智利 28（`south-america` 0→28，**首个南美洲国家**，该洲由此上线）
    · ✅ 匈牙利 26（`europe` 549→575，东欧第三国；旅游模式 1 概览 + 12 城）
    · ✅ 斯洛伐克 28（`europe` 575→603，东欧第四国；旅游模式 1 概览 + 8 城 + 1 条国内航线）
    · ✅ 斯洛文尼亚 28（`europe` 603→631，南欧第六国；旅游模式 1 概览 + 8 城；无可收录航线）
    · ✅ 马耳他 12（`europe` 631→643，南欧第七国，小国按 12 条口径；旅游模式 1 概览 + 9 城镇；无可收录航线）
    · ✅ 爱沙尼亚 26（`europe` 643→669，北欧次区域；旅游模式 1 概览 + 8 城 + 2 条国内航线 flight-unknown）
    · ✅ 拉脱维亚 27（`europe` 669→696，北欧次区域；旅游模式 1 概览 + 9 城；无可收录航线）
    · ✅ 罗马尼亚 29（`europe` 696→725，东欧次区域；旅游模式 1 概览 + 8 城；国际航线 `pek-otp`）
    · ✅ 保加利亚 27（`europe` 725→752，东欧次区域；旅游模式 1 概览 + 12 城；无可收录航线）
    · ✅ 乌兹别克斯坦 25（`asia` 349→374，中亚次区域第二国；旅游模式 1 概览 + 11 城 + 国际航线 `can-tas`）
    · ✅ 立陶宛 25（`europe` 752→777，北欧次区域；旅游模式 1 概览 + 9 城；无可收录航线）
    · ✅ 吉尔吉斯斯坦 25（`asia` 374→399，中亚次区域第三国；旅游模式 1 概览 + 11 城 + 国际航线 `pek-bsz` / `can-bsz`）
    · ✅ 斯里兰卡 25（`asia` 399→424，**开启南亚次区域** `southern-asia`；旅游模式 1 概览 + 12 城 + 国际航线 `tfu-cmb` / `ckg-cmb`）
    · ✅ 文莱 12（`asia` 424→436，东南亚次区域，小国 12 条口径；旅游模式 1 概览 + 9 城镇 + 国际航线 `pkx-bwn`）
  - **土耳其**：`country: "turkey"`（西亚首个国家，中亚之后第二个新开的亚洲次区域）；零争议红线
    从严执行——阿拉拉特山只作「土耳其最高峰 + 层状火山」的地质地理事实，不涉亚美尼亚民族象征 /
    种族屠杀议题，边境表述比照阿尔卑斯法意瑞交界的中性写法；东南安纳托利亚 / 库尔德相关地形一律
    不收；内姆鲁特火山（比特利斯，凡湖旁的火口湖）与阿德亚曼内姆鲁特山巨石人像遗址为同名不同地，
    条目内已注明区分。旅游模式因安全考量未收加济安泰普等靠近叙利亚/伊拉克边境的城市，以布尔萨/
    科尼亚替代。层状火山 / 山系→`mountain_system`、破火山口→`basin`、高原→`plateau`、
    钙化梯田 / 火山灰侵蚀地貌→`hills`、峡谷→`gorge`、海峡 / 深切海岸→`coast`、湖→`lake`、
    河源区→`river`、冲积平原→`plain`。
  - **克罗地亚**：`country: "croatia"`（南欧第五国，意大利/西班牙/希腊/葡萄牙之后）；
    `COUNTRIES` 按拼音插入最前（克罗地亚 Kèluódìyà < 葡萄牙 Pútáoyá），南欧注释块同步更新。
    达尔马提亚海岸按地学命名范例表述（"达尔马提亚型海岸"一词的原产地，与意大利卡尔索是"karst"
    原产地同一手法）；跨境地形（迪纳拉山、洛尼斯科波列等）一律不提及邻国名称，纯境内地貌表述——
    比意大利/法国的"与邻国交界"中性表述更进一步，因克罗地亚周边巴尔干国家均在安全排除名单内
    （见 `docs/expansion-playbook.md` §2.1），任何提及都可能读作立场表态，故直接省略。`history`
    段仅写地质史、广泛记载的古罗马/拜占庭/威尼斯共和国/哈布斯堡等前现代史，1990 年代及以后的
    政治/民族/战争议题一律不碰（零争议红线本轮特别强调项）。旅游模式里耶卡的 identity 段同样只写
    奥匈帝国港口史实，不写后续治理更迭。褶皱山系 / 孤立结晶岩残丘→`mountain_system`、
    潘诺尼亚沉积平原 / 三角洲 / 洪泛湿地→`plain`、达尔马提亚型淹没海岸 / 红土海岸 / 海湾→`coast`、
    钙华堤坝湖 / 喀斯特原野积水湖→`lake`、喀斯特河流→`river`、深切喀斯特峡谷→`gorge`、
    淹没山脊型岛屿→`island`。
  - **捷克**：`country: "czechia"`（东欧次区域第二国，波兰之后）；`COUNTRIES` 按拼音插入波兰
    之后（波兰 Bōlán < 捷克 Jiékè），东欧注释块同步更新。全境近乎被古老山地环绕（苏台德山系的
    克尔科诺谢山/克鲁什内山/耶塞尼克山/卢日采山 + 独立的舒马瓦山/新城山/摩拉维亚—西里西亚
    贝斯基德山），跨境峰顶（斯涅日卡峰、劳舍峰、卡梅内茨峰等）一律按"捷克与德国/波兰/奥地利
    交界"中性表述、不涉主权，如实标注在交界一带。布尔迪丘陵、波迪伊河谷的冷战边境管制历史
    按客观事实简述（人为活动限制客观促成生态保护），不碰现代政治评价；`history` 段仅写地质史、
    广泛记载的中世纪至近代史与 UNESCO 认定。旅游模式：1 概览 + 12 城（布拉格 / 布尔诺 /
    皮尔森 / 卡罗维发利 / 切斯基克鲁姆洛夫 / 库特纳霍拉 / 奥洛穆茨 / 捷克布杰约维采 / 特尔奇 /
    利托米什尔 / 俄斯特拉发 / 赫拉德茨-克拉洛韦）。**俄斯特拉发是本国唯一填 airport 的非首都城市**
    （OSR 以本城命名、由州所有、有定期客运航班）；赫拉德茨-克拉洛韦的最近定期航班机场是帕尔杜比采
    的 PED，按既定做法不填。两城在 howItWorks 里互指：同为「法定市」，俄斯特拉发之下有 23 个选举
    产生的区政府，赫拉德茨-克拉洛韦一个都没有、只有市参事会设立的咨询性地方委员会。
    国内航线：核实后捷克国内**无真实商业航空网络**——原捷克航空（ČSA）布拉格—布尔诺/俄斯特拉发/
    卡罗维发利三条国内航线已于 2024 年 ČSA 停止运营前更早即停飞，境内城市间以铁路/大巴为主，
    诚实起见本国**不收录任何国内航线**，不编造。褶皱古老山系→`mountain_system`、结晶岩基底出露
    准平原→`hills`、砂岩风化"岩城"→`hills`、构造断陷盆地→`basin`、喀斯特台地→`plateau`、
    地表下切峡谷→`gorge`、低平冲积低地→`plain`、干流河段→`river`、人工蓄水湖/水库→`lake`、
    深切曲流→`valley`。
  - **匈牙利**：`country: "hungary"`（东欧次区域第三国，波兰、捷克之后）；**地形 26 条已完成**，
    旅游模式首批 8 城 + 国家概览（布达佩斯 / 德布勒森 / 塞格德 / 佩奇 / 杰尔 / 埃格尔 /
    肖普朗 / 圣安德烈）中英双语 7 段 + 42 个 POI；`COUNTRIES` 按拼音
    插在捷克之后（捷克 Jiékè < 匈牙利 Xiōngyálì），东欧注释块同步更新。匈牙利是喀尔巴阡盆地
    的中心，**七成以上国土是海拔 200 米以下的平原**，山地只沿北缘与西南零星分布 —— 这份地形集
    的主线因此不是「高度」，而是「一个内陆沉降盆地怎么被河流填平、又被火山与石灰岩在边缘打断」。
    跨境地形（泽姆普伦山北端的纳吉米利奇峰、克塞格山、费尔特湖）一律按「匈牙利与奥地利/
    斯洛伐克交界」中性表述，**不涉主权、不描述边界线**；`history` 段只写地质史、广泛记载的
    前现代史与 UNESCO 认定，**二十世纪的政治 / 领土议题一律不碰**（零争议红线本轮特别强调项）。
    火山山地 / 石灰岩山地→`mountain_system`、喀斯特台地→`plateau`、沉积大平原→`plain`、
    puszta 草原→`grassland`、湖→`lake`、河→`river`、峡谷式河段→`gorge`、玄武岩残丘→`inselberg`。
    **航线**：核实后匈牙利**无定期国内客运航线**（MALÉV 的国内线 1969 年即停飞，此后地区机场
    未升级、机队大型化使国内线不再有商业可行性），按捷克的既有处理**如实不收录、不编造**；
    中国—布达佩斯有 7 条不经停直飞，按「两个来源指向同一个号」只收证据最硬的
    **广州—布达佩斯 CZ649/CZ650**（广州市交通运输局官网通报 + 携程时刻表双源）。
  - **斯洛伐克**：`country: "slovakia"`（东欧次区域第四国，波兰、捷克、匈牙利之后）；
    `COUNTRIES` 按拼音插在捷克与匈牙利之间（捷克 Jiékè < 斯洛伐克 Sīluòfákè < 匈牙利 Xiōngyálì）。
    **零争议红线本轮的特别项是东北角**：波洛尼尼国家公园三面临国界，比照克罗地亚的更严做法 ——
    **一个邻国国名都不提**，注册表与正文一律只写「国土东北端」「国界交汇处」；塔特拉与杜纳耶茨
    河峡谷这类与波兰共有的地形按「斯洛伐克与波兰交界一带」中性表述、不涉主权、不描述边界线。
    `history` 段只写地质史、广泛记载的牧业 / 矿业 / 登山史与国家公园设立年份。
    高山结晶岩核心 / 火山山地 / 石灰岩中山→`mountain_system`、喀斯特高原→`plateau`、
    深切喀斯特峡谷与深切曲流→`gorge`、砾岩岩城→`hills`、冲积低地→`plain`、
    山间构造盆地→`basin`、干流河段→`river`、冰碛堰塞湖与山崩堰塞湖→`lake`、河间岛地→`island`。
    **`high-tatras` 与波兰侧既有的 `tatra-mountains` 是同一条山脉的两个不同对象**（锚点各为
    本国最高点），注册表 `source` 里互相点名，不要互相覆盖；`slovensky-kras` 与匈牙利侧的
    `aggtelek-karst`、`zitny-ostrov` 与匈牙利侧的 `szigetkoz` 同理。
    旅游模式：1 概览 + 8 城（布拉迪斯拉发 / 科希策 / 日利纳 / 班斯卡比斯特里察 / 波普拉德 /
    班斯卡什佳夫尼察 / 巴尔代约夫 / 莱沃恰），人口一律用 ŠÚ SR **2025-12-31** 的市域登记人口。
    机场如实处理：日利纳（ILZ）与斯利亚奇（SLD）无稳定定期客运航班，**不填 airport 字段**；
    波普拉德—塔特拉（TAT）有机场但定期航班很少。国内航线只有一条 `bts-ksc`
    （2025-11 由 Wizz Air 复航），航班号与机型未核实，按新增的 `flight-unknown` 状态处理。
  - **斯洛文尼亚**：`country: "slovenia"`（南欧次区域第六国，按联合国 M49 归南欧）；`COUNTRIES` 按拼音插在
    葡萄牙与西班牙之间（葡萄牙 Pútáoyá < 斯洛文尼亚 Sīluòwénníyà < 西班牙 Xībānyá）。
    **零争议红线本轮的特别项是南缘与海岸**：南/东南方向的邻国**一个国名都不提**（比照克罗地亚、
    斯洛伐克东北角）；海岸条目与皮兰、科佩尔**不写海岸线长度、不写海湾、不涉任何海域划界**；
    与意大利、奥地利共有的地形（尤利安阿尔卑斯山西段、卡拉万克山、喀斯特高原、布尔达丘陵）
    按「交界一带」中性表述。`history` 段只写地质史、史前与中世纪至近代史、**一战索查河战线的简短事实**
    （比照意大利多洛米蒂的一战山地战）、保护区年份与 UNESCO；一战之后的政治 / 领土 / 民族议题一律不碰。
    石灰岩高山 / 结晶岩中山 / 迪纳拉喀斯特山地→`mountain_system`、喀斯特高原与高山牧场台地→`plateau`、
    复理石丘陵与第三纪低丘→`hills`、构造沼泽洼地→`basin`、冲积平原→`plain`、干流与喀斯特伏流河→`river`、
    冰蚀谷与塌陷谷→`valley`、隘谷与地下河峡谷→`gorge`、冰川湖与间歇湖→`lake`、复理石海崖→`coast`。
    **`kras-plateau` 与意大利侧既有的 `carso-karst` 是同一片高原的两侧**，`sava-river-slovenia` 与
    `sava-river-croatia` 同理，注册表 `source` 里互相点名。
    旅游模式：1 概览 + 8 城（卢布尔雅那 / 马里博尔 / 采列 / 克拉尼 / 科佩尔 / 皮兰 / 布莱德 / 普图伊），
    人口一律用 SURS **2026-01-01** 的市镇（občina）人口，**直接从 SiStat 的 PX-Web API 取数**
    （表 05E1022S，2026H1；`curl https://pxweb.stat.si/SiStatData/api/v1/en/Data/05E1022S.px` 取元数据后 POST 查询）——
    比网页检索快、也不占联网检索额度。机场如实处理：马里博尔 MBX 与皮兰市镇内的波尔托罗日机场
    无稳定定期客运航班，**不填 airport 字段**；克拉尼离 LJU 很近但机场不在其市域内，也不填。
    **航线：一条都不收** —— 斯洛文尼亚境内无定期国内客运航班；中国与斯洛文尼亚之间**从未开通过
    定期不经停航班**（EX-YU Aviation 2025-04 报道卢布尔雅那机场仍在寻求中国航线），
    按卢森堡 / 冰岛 / 克罗地亚的既有处理如实记为缺口，不编造。
  - **马耳他**：`country: "malta"`（南欧次区域第七国）；`COUNTRIES` 按拼音插在克罗地亚与葡萄牙之间
    （克罗地亚 Kèluódìyà < 马耳他 Mǎ'ěrtā < 葡萄牙 Pútáoyá）。国土约 316 平方公里，地形按卢森堡 / 新加坡的 **12 条**口径。
    **地形主线只有一条**：五套渐新世—中新世岩层（下珊瑚灰岩 / 抱球虫灰岩 / 蓝黏土 / 绿砂层 / 上珊瑚灰岩）
    + 群岛向东北倾斜 —— 西南抬起成崖（丁利悬崖；蓝黏土出露处变成马伊斯特拉尔的崩积巨石坡 rdum），
    东北下沉成溺谷港湾（大港、加斯里谷），溶洞塌陷形成圆形海湾（德维耶拉、蓝洞），大断层以北为地垒—地堑岭谷。
    主要来源是马耳他大学 Gauci《Rocks》（2025，UM OAR 可 curl + pdftotext）与 IRMCo 戈佐电子教材；
    **ERA、NSO、Planning Authority 等 gov.mt 站点拒绝脚本抓取**，要么用检索摘要、要么在浏览器里打开。
    **零争议红线本轮的特别项是菲尔夫拉岛**：该岛曾出现在国际法院的大陆架案里，条目**一个字都不写海域 / 大陆架**；
    旅游模式不写移民与外籍人口比例、建设开发争议。`history` 只到骑士团、英国时期、保护区设立与 UNESCO。
    海崖 / 溺谷港湾 / 塌陷海湾 / 沙滩沙丘 / 崩积坡海岸→`coast`、断层崖与地垒岭谷 / 台地丘陵→`hills`、小岛→`island`、深切溺谷→`gorge`。
    旅游模式：1 概览 + 9 城镇（瓦莱塔 / 斯利马 / 圣朱利安斯 / 姆迪纳与拉巴特 / 比尔古（三城）/ 维多利亚（戈佐）/
    马尔萨什洛克 / 梅利哈 / 圣保罗湾）。**马耳他没有「市」一级政府**：68 个地方议会辖区（locality）+ 2021 年起六个大区议会，
    所以「城市」条目都是 locality；首都瓦莱塔只有约 5,200 人，人口最多的辖区是圣保罗湾（约 4.3 万）——这是 howItWorks 的主线。
    人口统一取 NSO NR 120/2026（2026-07-09 发布）**表 11 的 2025-12-31 分地方人口**，官网 403，
    在浏览器 pane 里 fetch 官方 xlsx 后手写 zip 解析取数（见 `docs/data-refresh-calendar.md`）。
    唯一民用机场为卢卡的 MLA，戈佐岛无定期航班、**不填 airport 字段**。机场—瓦莱塔直达公交 2025-04 起是 **TD4**（不是 X4）。
    **航线：一条都不收** —— 中国—马耳他查无定期不经停客运航班，马耳他与戈佐之间无定期航班。
  - **爱沙尼亚**：`country: "estonia"`（联合国 M49 归**北欧**次区域）；`COUNTRIES` 按拼音插在爱尔兰与冰岛之间
    （爱尔兰 Ài'ěrlán < 爱沙尼亚 Àishāníyà < 冰岛 Bīngdǎo）。地形主线：北岸奥陶纪石灰岩台地的**北爱沙尼亚陡崖**（与瀑布）、
    南部冰川堆积高地（哈尼亚 / 奥泰佩 / 卡鲁拉 / 萨卡拉 / 沃雷马鼓丘群 / 潘迪弗雷）、中西部沼泽国家公园（苏马 / 阿卢塔古塞 / 科尔韦马）、
    **冰后期陆升塑造的西爱沙尼亚低地与群岛**（萨列马 / 希乌马 / 维尔桑迪 / 马察卢湾）、卡利陨石坑、楚德湖与维尔茨湖、三条河。
    主要来源：爱沙尼亚百科 entsyklopeedia.ee（**https 证书有问题，用 http 直接 curl**）、保护区管理方 kaitsealad.ee / EELIS、Riigi Teataja。
    **零争议红线本轮的特别项是东部**：楚德湖只写湖泊地理（三部分、出水口纳尔瓦河、皮里萨尔岛面积变化），**不写边界、条约与政治**；
    正文与旅游模式**不写族群构成、语言政策、苏联时期政治叙事**，纳尔瓦等边境城市本批不收。
    旅游模式：1 概览 + 8 城（塔林 / 塔尔图 / 派尔努 / 库雷萨雷（萨列马岛）/ 哈普萨卢 / 维尔扬迪 / 拉克韦雷 / 奥泰佩）。
    人口一律取**统计局 RV0240 的 2026-01-01 数**（PxWeb API `andmed.stat.ee/api/v1/en/stat/RV0240`，POST 查询），
    2017 年市镇改革后很多镇并入乡（vald），**镇与所属乡、市与城区本身分开写**（库雷萨雷镇 1.27 万 / 萨列马乡 2.99 万）。
    机场：塔林 TLL、塔尔图 TAY（只飞赫尔辛基）、库雷萨雷 URE；其余城市**不填 airport**，正文写「没有定期航班的机场」（拉克韦雷、奥泰佩都有小机场）。
    **航线**：`tll-ure`、`tll-kdl`（NyxAir 按国家合同执飞，航班号 / 机型未取到两个独立来源 → `flight-unknown`，解说不点名承运人航班号机型）；
    中国—塔林查无定期不经停客运航班，如实不收。
  - **拉脱维亚**：`country: "latvia"`（联合国 M49 归**北欧**次区域）；`COUNTRIES` 按拼音插在芬兰与挪威之间
    （芬兰 Fēnlán < 拉脱维亚 Lātuōwéiyà < 挪威 Nuówēi）。地形主线：东半部冰川「岛状」堆积高地（维泽梅 / 阿卢克斯内 / 拉特加莱，
    全国最高点盖津卡尔恩斯山约 312 米）、东南的边缘型终碛高地（奥格什泽梅）、西部基岩高地（西库尔萨），高地之间的东拉脱维亚低地与泽姆加莱平原，
    沿海滨海低地与里加湾沙岸、斯利泰雷古岸崖、于尔卡尔内海崖、维泽梅石质海岸；沿基岩古河谷流动的高亚 / 阿巴瓦 / 道加瓦（曲流），
    文塔瀑布；湖泊按三种口径各有第一（面积卢班斯、水体积拉兹纳、深度德里济斯）；四条主干河。
    主要来源：拉脱维亚国家百科 enciklopedija.lv（curl 可取，2 级，条目作者为拉脱维亚大学地理学者）、自然保护局 tiekamiesdaba.lv（**daba.gov.lv 常连不上，同一机构的旅游门户可取**，1 级）、UNESCO。
    **零争议红线本轮的特别项是东部与东南部**：拉特加莱高地、奥格什泽梅高地、道加瓦河、德里济斯湖只写「国土东部 / 东南部」，**不写邻国与界线**；
    道加瓦河只写境内河段、发源地只说「境外」；普拉维尼亚斯水电站只写工程事实，被淹没的河崖不点名；斯利泰雷不写族群与封闭边境区；
    西库尔萨高地最高点用百科给出的并用名 Radiņkalns。旅游模式不写族群构成、语言政策、两次世界大战与苏联时期叙事，陶格夫匹尔斯只写城市结构、要塞建筑与罗斯科。
    旅游模式：1 概览 + 9 城（里加 / 陶格夫匹尔斯 / 利耶帕亚 / 叶尔加瓦 / 尤尔马拉 / 文茨皮尔斯 / 锡古尔达 / 采西斯 / 库尔迪加）。
    人口一律取**中央统计局 CSP 表 IRS031 的 2026-01-01 数**（PxWeb API `data.stat.gov.lv/api/v1/en/OSP_PUB/START/POP/IR/IRS/IRS031`，POST json-stat2）；
    **2021 年改革后 42 个地方政府 = 7 个国家城市自己的政府 + 35 个市镇（novads）**（2025-07-01 瓦拉克利亚尼并入马多纳后），
    很多市镇的行政中心设在国家城市里却是两个政府（叶尔加瓦市 / 叶尔加瓦市镇），镇与所属市镇的人口分开写（锡古尔达镇 1.48 万 / 市镇 3.15 万）；
    目录 admin1 用 5 个**规划区域**分组（规划区域不是一级政府，正文不称省 / 州）；历史地区（维泽梅等）的归属有过立法争论，**正文不写历史地区归属**。
    机场：只有里加 RIX 有定期客运航班（且在马鲁佩市镇、不在里加市域），利耶帕亚 LPX 2020 年 9 月起、文茨皮尔斯 VNT 长期无定期航班，**都不填 airport**。
    **航线：一条都不收** —— 国内无定期客运航班；中国大陆—里加查无定期不经停客运航班，如实记为缺口。
    中文译名：Daugavpils 用中文维基「陶格夫匹尔斯」、Jelgava 用「叶尔加瓦」。
  - **罗马尼亚**：`country: "romania"`（东欧次区域）；`COUNTRIES` 按拼音插在捷克与斯洛伐克之间（捷克 Jiékè < 罗马尼亚 Luómǎníyà < 斯洛伐克 Sīluòfákè）。
    地形主线：喀尔巴阡弧（东喀罗马尼亚段 / 南喀 / 阿普塞尼）三面围住特兰西瓦尼亚盆地，外侧副喀尔巴阡丘陵（布泽乌泥火山），
    再外是西部平原、罗马尼亚平原与多布罗加古老台地，东南端多瑙河三角洲与黑海沿岸；冰斗湖山块（弗格拉什 / 雷泰扎特 / 罗德纳）、
    火山（克利马尼、圣安娜火山口湖）、悬空向斜（恰赫勒乌）、喀斯特与冰洞（阿普塞尼）、峡谷（铁门 / 比卡兹 / 图尔达）、四条河。
    **数字底座是 INS《罗马尼亚统计年鉴》第 1 章表 1.3–1.6**（山峰 / 河流 / 天然湖 / 人工湖，表源科学院地理研究所）；
    INS 官网、ANANP、legislatie.just.ro 多有浏览器验证或拒连，年鉴读的是第三方镜像的原版 PDF，**锚点坐标一律取 OSM**（Wikidata 的托阿卡峰、巴贝莱岩坐标都是错的）；
    Enciclopedia României 是志愿编辑 wiki，只作旁证。「西喀尔巴阡」在国际与罗马尼亚两套体系里指不同的山，不用作 id。
    **零争议红线本轮的特别项是界河与边境**：多瑙河只写境内段（锚点选两岸都在境内的布勒伊拉）、铁门峡只写罗马尼亚岸、三角洲不写跨境比例、
    黑海沿岸不写海域划界与岛屿、普鲁特河不收；「摩尔多瓦高原」中文名易被读作摩尔多瓦共和国且来源薄弱，不收；
    特兰西瓦尼亚与哈尔吉塔一带不写族群、外语地名与归属史；布切吉「斯芬克斯」只写风化岩。
    旅游模式：1 概览 + 8 城（布加勒斯特 / 克卢日-纳波卡 / 蒂米什瓦拉 / 布拉索夫 / 锡比乌 / 康斯坦察 / 奥拉迪亚 / 锡吉什瓦拉）；
    **紧邻乌克兰、摩尔多瓦边境的图尔恰（约 7 km）与雅西（约 15 km）不收**。
    **人口口径是本国最大的坑**：INS TEMPO 的 POP107D 是**户籍人口**（不看实际常住地，布加勒斯特 210.8 万），城市级**常住人口只有普查才有**（2021 普查布加勒斯特 171.7 万），
    所以城市一律写「2021 年人口普查常住人口」，全国写 2026-01-01 常住人口 1904 万（INS 新闻稿 8 月底发布）。
    布加勒斯特是与县同级的市，总市长 + 总议会 + 6 个扇形分区，总议会决定对各区有约束力；每县有政府任命的省长（prefect），与地方议会无隶属。
    有国内定期航班（克卢日 / 蒂米什瓦拉 / 奥拉迪亚往返布加勒斯特），按规则 ⑫ 不列航点；OTP 在伊尔福夫县奥托佩尼、机场公交 783 路 2023-12 起改 100 路。
    **航线 `pek-otp`（2026-09-15 收录）**：国航北京首都—布加勒斯特 2026-09-04 起每周三班、真不经停（之后同号续飞萨格勒布，所以克罗地亚一侧仍记「经停」），
    CA913 与 A330-200 由多个互不相同的来源确认；航点走哈萨克斯坦—里海—黑海一线是示意，`source.note` 写明不代表实际航路。
  - **保加利亚**：`country: "bulgaria"`（东欧次区域）；`COUNTRIES` 按拼音插在东欧组最前（保加利亚 Bǎojiālìyà < 波兰 Bōlán）。
    地形主线是自北向南的东西向平行带：多瑙河平原 / 巴尔干山脉（中巴尔干国家公园）/ 山前盆地（索非亚、卡赞勒克）/ 斯雷德纳山 / 上色雷斯平原 /
    里拉—罗多彼地块（里拉与七湖、皮林、维托沙、罗多彼），东临黑海（卡利阿克拉、布尔加斯湖群），加上岩柱地貌（贝洛格拉奇克、梅尔尼克、石林）与四条河。
    **数字底座是 NSI《Statistical Reference Book 2025》地理章节**（峰高 / 河长 / 天然湖 / 海岸线，表源地籍测绘局 GCCA）——**同一本年鉴里伊斯克尔河正文 368 km、表 3 352.3 km**，
    河长整体都是「NSI 表 / 保加利亚语维基」两套数，伊斯克尔与扬特拉正文不写河长；保护地面积一律取执行环境署登记册 EEA-ZT 的现值（里拉 2024、中巴尔干 2026 都有重测）。
    **多瑙河保加利亚段全是界河，不单列**（罗马尼亚 `danube-romania` 已覆盖），`dobrudzha-plateau` 与罗马尼亚 `dobrogea-plateau`、`black-sea-coast-bg` 与 `black-sea-coast-ro` 互相点名。
    **零争议红线本轮的特别项**：多布罗加归属史、罗多彼的族群宗教与地名改名史、皮林—斯特鲁马河谷相关的区域政治名称（**全篇不出现「马其顿」**，普罗夫迪夫只写腓力二世）、
    卡利阿克拉与佩雷利克的军事设施、宗教场所与传说、1877–1878 年战争与「解放」叙事、社会主义时期纪念建筑一律不写；斯特兰贾只写境内、边界山脊上的最高峰不点名。
    旅游模式：1 概览 + 12 城（索非亚 / 普罗夫迪夫 / 瓦尔纳 / 布尔加斯 / 大特尔诺沃 / 鲁塞 / 内塞伯尔 / 班斯科 / 卡赞勒克 / 特里亚夫纳 / **舒门 / 弗拉察**）；鲁塞在界河边照收但不写对岸，里拉修道院不作城市条目。
    **舒门 / 弗拉察（2026-09-16）**把覆盖推到 11 个州，两条 howItWorks 都是仓库此前没写过的结构层次：舒门市镇下辖的 **26 个村各自是有直选村长的「村政区（кметство）」**（世界遗产马达拉骑士就在其中的马达拉村，岩雕本身归国家级保护区 —— 一块岩壁上叠着三层）；弗拉察则是**同一座山上两个政府** —— 自然公园由林业执行署下属的国家管理局管，园内的莱德尼卡洞自 2010-03 起由市政府经营。⚠️ 莱德尼卡「离弗拉察 16 公里」是**盘山公路里程、直线只有 4.4 公里**，正文两个数都写、各注口径，否则 `check:bearings` 判不符。两城的机场都是 `scheduled_service=no`，按规则 ⑬ 写「没有定期航班的机场」、不填 airport。
    人口取 **NSI 2025-12-31 年度数**（常住口径、按申报现住址分到地方，次年 4 月底发布），**城市与市镇分开写**（索非亚城 121.3 万 / 首都市镇 130.4 万）；
    **内塞伯尔年度数比 2021 年普查高出五成、原因未查明，改用普查数**。结构性事实：首都市镇自成「索非亚市州」、**索非亚州不含索非亚**；
    只有索非亚 / 普罗夫迪夫 / 瓦尔纳分区（区长直选），布尔加斯不到 30 万不分区；金沙滩、阳光海滩等 8 处国家级度假区在统计上**没有常住人口**。
    **2026-01-01 起使用欧元**（过渡期细节均已结束，正文不写）。有国内定期航班（瓦尔纳 / 布尔加斯往返索非亚），按规则 ⑫ 不列航点；
    **航线：一条都不收** —— 中国大陆—索非亚查无定期不经停客运航班；国内两条的航班号只有瓦尔纳机场屏一个来源，待补第二来源后再收。
  - **立陶宛**：`country: "lithuania"`（联合国 M49 归**北欧**）；`COUNTRIES` 按拼音插在拉脱维亚与挪威之间（Lātuōwéiyà < Lìtáowǎn < Nuówēi）。
    地形主线：**整个地表都是冰川留下的**——西部热马伊蒂亚高地与东南弧形的波罗的高地（苏杜瓦 / 祖基亚 / 奥克什泰蒂亚）夹着中立陶宛低地，
    更东是倒数第二次冰期的古老高地（梅德宁凯 / 斯文乔尼斯，湖泊已消失）与东南沙质平原（内陆沙丘、松林）；西部滨海低地、库尔斯沙嘴与潟湖、涅穆纳斯三角洲；
    深切曲流（涅穆纳斯大弯）、冰下河道湖（陶拉格纳斯、特拉凯、普拉特利艾）、北部石膏喀斯特塌陷坑（比尔扎伊）、冰碛海崖（荷兰人帽）、沼泽化湖与泥炭沼泽（祖文塔斯 / 卡马诺斯）。
    **1 级数字走两个开放 WFS**：统计局与地质调查局网站被 Cloudflare 挡，但国家保护区地籍 STVK（`services.stvk.lt/wms/stvk-services`，保护地面积、设立文件与**法定设立目的**）
    与河流湖泊地籍 UETK（`gis.biip.lt/qgisserver/uetk_public`，**必须带浏览器 User-Agent**，河流境内长度与湖泊地籍多边形面积）可以直接 curl；
    **法定设立目的里自带的名次可以当 1 级用**（陶拉格纳斯「立陶宛最深的湖」、普拉特利艾「热马伊蒂亚最大最深」、卡马诺斯「北立陶宛黏土平原最大沼泽复合体」），
    但同一段文字里的「独一无二 / 最有价值」是法律修辞，不照抄。UETK 湖泊面积是地籍多边形口径，与百科差 1–3%，正文写「约」。
    **零争议红线本轮的特别项是东、南、西南三面**：邻国名、界河长度、沙嘴与潟湖的另一侧、三角洲主汊国界与「左岸 / 右岸」一律不写；
    **全国最高点奥克什托亚斯山离国界约 2.7 km，不作锚点**（改用同一高地、只差 0.27 m 的克鲁奥皮内山），「最高点」只有立陶宛通用百科 2 级来源、正文带「据」；
    苏联时期机构名、冷战遗址、少数民族、宗教建筑群与传说不写。
    旅游模式：1 概览 + 9 城（维尔纽斯 / 考纳斯 / 克莱佩达 / 希奥利艾 / 帕内韦日斯 / 特拉凯 / 帕兰加 / 尼达 / 比尔什托纳斯），人口取统计局 SDMX 表 S3R167 的 2026-01-01 数，
    「XX 市市镇」与同名「XX 区市镇」是两个政府；德鲁斯基宁凯（离国界约 6.7 km）不收。**航线：一条都不收** —— 无国内定期客运航班，中国大陆—维尔纽斯查无不经停定期航班。
    （旅游部分 2026-09-15 先于地形写完并核实，暂存到地形研究完成后一起并入。）
  - **乌兹别克斯坦**：`country: "uzbekistan"`（中亚次区域，哈萨克斯坦之后）；`COUNTRIES` 按拼音排在哈萨克斯坦之后（Hāsàkèsītǎn < Wūzībiékèsītǎn）。
    地形主线：国土约五分之四是平原（政府门户 1 级），东部天山西段（恰特卡尔 / 大钦甘 / 库拉马）与帕米尔—阿赖西段（吉萨尔 / 拜孙套 / 努拉套），
    最大的山间盆地费尔干纳（政府门户原话）；中西部克孜勒库姆沙漠与残山（塔姆德套 / 苏尔坦乌瓦伊斯）、绿洲冲积平原（布哈拉 / 花剌子模 / 卡尔希 / 米尔扎丘尔）、
    阿姆河三角洲与锡尔河、「流不到终点」的泽拉夫尚河；咸海一组（南咸海 / 阿拉尔库姆 / 乌斯秋尔特东部 / 苏多奇耶湖）与 1969 年后形成的艾达尔—阿尔纳赛湖系；津济尔班峡谷的埃姆斯阶「金钉子」。
    **数字底座很薄**：统计委员会年鉴取不到，1 级只有 gov.uz（国土、平原比例、最高点 4,643 m、最大山间盆地）、lex.uz（保护地设立）、UNESCO / 拉姆萨尔 / 国际地层委员会；
    河长与山脉长度多来自苏联大百科（2 级，1970 年代）与俄语维基（3 级），湖泊现状面积一律不写。
    **零争议红线本轮的特别项是东部国界山与水**：主脊国界上的峰（含全国最高点）不作锚点、不写位置；普斯克姆山、扎敏山（锚点贴界）与泽拉夫尚山西段（无来源）不收；
    费尔干纳盆地不写飞地与三国分属；阿姆河界河段、铁尔梅兹不收；上游国家、水量分配、水库调度与咸海归责主体一律不写；卡拉卡尔帕克斯坦只作地名。
    旅游模式：1 概览 + 11 城（塔什干 / 撒马尔罕 / 布哈拉 / 希瓦 / 浩罕 / 沙赫里萨布兹 / 卡尔希 / 马尔吉兰 / 努拉塔 / **纳曼干 / 吉扎克**，后四城 2026-09-16 补）。**纳曼干（2026-09-16）推翻了上一轮「地标无来源」的判断** —— 官方旅游门户逐个点名了经学院、陵墓与公园；但仍有 4 处官方点名、OSM 与 Wikidata 都查不到坐标的对象，**如实不写、不拿描述倒推坐标**。纳曼干的 howItWorks 主线：**市里只有两个区、都在外围**（2020 / 2021 年设），其余马哈拉连同老城中心不属于任何区、由市直辖；同名的「纳曼干区」是另一个政府、中心在市外。吉扎克：**围着市的那个区不叫吉扎克区**（2017 年起叫沙罗夫·拉希多夫区、人口比市还多），且**全州唯一有定期航班的机场不在州府**、在东南约 52 km 的扎敏 —— ⚠️ **扎敏机场（OMN）根本不在 OurAirports 数据里**，官方机场清单与航班屏上却有定期航班，是「OurAirports 的错法是滞后」的又一实例（同浩罕 OQN / 沙赫里萨布兹 RBZ）；**费尔干纳市（离国界约 15 km）、奇尔奇克不收**；**铁尔梅兹、安集延不收，努库斯（行政地位敏感、地标少）与纳曼干（地标无来源）本批不收**。
    人口口径：全国写 **2026 年普查初步结果**（时点 2026-01-15，约 3905 万，初步数可能修订），城市写**统计委员会 2026 年初估算**（开放数据 xlsx 246），两个口径不在一句里比。
    结构性事实：州与塔什干市的 hokim 由总统任免、区与市的由州级任免并经议会批准；马哈拉是选举主席、不属于国家机关的公民自治单位；塔什干市与塔什干州分开；
    希瓦市、沙赫里萨布兹市都是 2018 年才设为市、与同名区平级；布哈拉火车站在卡甘；抵达 3 天内须登记居留。
    机场：浩罕 OQN、沙赫里萨布兹 RBZ 在 OurAirports 标无定期航班，但 2026-09 机场航班屏有 Silk Avia 往返塔什干 —— **数据集滞后的实例，按机场屏填**；希瓦填乌尔根奇 UGC（直线约 33 km）。
    **航线**：收 `can-tas`（南航 CZ3053，国资委官网 + 塔什干机场屏双 1 级，A330-300，时长两源一致）；**`pek-tas`（2026-09-16 收）**：国航 CA777 北京首都 PEK—塔什干，首都机场官网航班实况 + 塔什干机场屏双 1 级，波音 737，计划 415 分钟（开航稿「逾 6.5 小时」是首航实飞）；
    西安—撒马尔罕 GJ8937（机型只有追踪站、2024 年曾是包机）、南航大兴—塔什干 CZ6027（PKX 与机型缺 1 级）暂不收。
  - **吉尔吉斯斯坦**：`country: "kyrgyzstan"`（中亚次区域第三国）；`COUNTRIES` 按拼音插在哈萨克斯坦与乌兹别克斯坦之间（Hāsàkèsītǎn < Jí'ěrjísīsītǎn < Wūzībiékèsītǎn）。
    **开工前先读英国外交部旅行建议**（2026-09-14 更新：无「建议勿前往」区域，只提示吉塔边境巴特肯州一带谨慎、可能有未爆弹药）。
    地形主线：约九成国土海拔 1,500 m 以上（国家统计委员会）；天山北部（吉尔吉斯山 / 塔拉斯阿拉套 / 昆格 / 泰尔斯凯）夹着伊塞克湖盆地与楚河、塔拉斯两块边缘低地，
    中天山伊内尔切克冰川区，内天山（阿特巴什山 / 3,500–4,000 m 的古夷平面「夏尔特」/ 阿克希拉克冰川山块 / 朱姆加尔盆地 / 苏萨梅尔谷地），西南费尔干纳山脉，南部阿赖山与阿赖谷地；
    湖：伊塞克湖 / 松湖 / 恰特尔湖（国家统计委员会湖泊水面面积口径的前三）、萨雷切列克（山崩堰塞湖，「西天山」世界遗产组成部分）、阿拉库尔、托克托古尔水库；纳伦河、博姆峡谷、阿拉阿尔恰、杰特奥古兹红岩。
    **1 级数字只有一个来源**：国家统计委员会《环境 2020–2024》汇编（**域名 stat.gov.kg，旧 stat.kg 连不上**；国土、湖泊面积、保护地 2024 年面积）；法规库 cbd.minjust.gov.kg 带 reCAPTCHA 取不到，
    保护地设立年份只有 2–3 级；**统计委员会列的河长（纳伦 535 / 楚河 221 km）没写口径、疑为境内长度，正文不引**；山脉长度与岩性来自苏联大百科（2 级）。
    **零争议红线本轮的特别项是国界山与界河**：天山最高峰、汗腾格里峰、外阿赖山最高峰、马纳斯峰都在国界上 —— 不作锚点、不写位于哪两国之间、不写命名史；
    天山最高峰海拔与中国 `tianshan` 条目口径说明一致（国测局 7,443 m / 国际常用 7,439 m）；巴特肯州与吉塔边境、阿克赛谷地、科克沙尔套、吐尔尕特口岸不收；
    楚河中游是界河，楚河谷地只写境内、bbox 不越过楚河；河流一律不写出境与上下游国家（纳伦河当批核实时抓到一句「下游与邻国河流衔接」已删）；不写矿山、伊塞克湖军事设施、托克托古尔水位与能源问题、以政治人物命名的峰。
    `inylchek-glacier` 与中国 `tianshan`、`kungey-ala-too` 与哈萨克斯坦 `zailiysky-alatau`、`sary-chelek` 与乌兹别克斯坦 `chatkal-range` 在 `source` 里互相点名。
    旅游模式：1 概览 + 11 城（比什凯克 / 卡拉科尔 / 乔尔蓬阿塔 / 巴雷克奇 / 纳伦 / 塔拉斯 / 科奇科尔 / 博孔巴耶沃 / 阿特巴什 / **卡拉巴尔塔 / 托克托古尔**，后四处 2026-09-16 补，南向口岸与边境地带一律不写）。**卡拉巴尔塔与托克托古尔互为对照**：本国的「市」不是一个级别而是两种 —— 前者在统计表里与区平级（州级市，而日阿伊尔区的区政府就设在城里、这座城却不属于该区），后者列在区之内（区属市，市政府与区国家行政机关同城两址）。两城的建成地标都很少（卡拉巴尔塔只有三处可写，城北的卫国战争题材公园与博物馆按红线不收；托克托古尔一座建成古迹都没有），正文按「制度 + 公路 + 地景」定基调、**不凑数**；新增分区键 `kg-west`。托克托古尔**不点名最近的有定期航班机场** —— 直线最近的两个所在城市离国界都不足 10 km、仓库本来就不收也不写；**离国界 < 20 km 的奥什、贾拉拉巴德（统计表已写作马纳斯市）、托克莫克、凯明不收**，比什凯克（约 17 km）首都照收但不写国界；
    布拉纳塔、碎叶等古迹与玛纳斯宫帐离国界太近不作 POI。人口取**统计委员会表 769 / 825 的 2026-01-01 常住人口估算**（xls 可直接下载；村一级是村政府数据），比什凯克与州同级、不属楚河州，纳伦州与塔拉斯州「一州一城」。
    **玛纳斯国际机场 IATA 代码 2025-08-09 起是 BSZ（不是 FRU）**，航线 id 用 `-bsz`；纳伦、塔拉斯机场有 Asman 定期航班但无 IATA 码，不填 airport；塔姆奇（伊塞克湖国际机场）只有夏季季节航班，乔尔蓬阿塔不填。
    **2026-01-01 起餐厅不得单列服务费** —— 与乌兹别克斯坦正好相反，规则 ⑪ 的正例。塔拉斯公路先过吐阿舒山口进苏萨梅尔谷地、再过奥特莫克山口下到塔拉斯河谷（初稿写反，当批核实改正）。
    航线：`pek-bsz` 国航 CA893 A321、`can-bsz` 南航 CZ8147 737-8；乌鲁木齐 CZ6005 2026-09-16 按 `flight-unknown` 收为 `urc-bsz`（追踪站对这条线标出南航机队里不存在的机型）；北京大兴 CZ5067 与已有 `pek-bsz` 路径几乎重合、机型未核，暂不收；成都、西安长龙航线第二来源不足不收。
    译名：Jumgal 用「朱姆加尔」（不用「准加尔」，免与准噶尔混淆）；Ala-Kul 用「阿拉库尔湖」（免与哈萨克斯坦阿拉湖混淆）。童话峡谷、阿尔斯兰博布、琼凯明谷地来源不足本批不收。
  - **斯里兰卡**：`country: "sri-lanka"`（南亚次区域第一国）。地形主线：锚形的中央山地（皮杜鲁塔拉格勒 2,524 m；1,000 m 以上只占国土约 3%，几乎所有河流发源于此）被占全岛约三分之二的低平原环绕，
    平原上散布残丘（锡吉里耶、利蒂加拉）；马哈韦利河是放射状水系的例外（先东、再北、再东入海）；**岛上没有天然湖泊**，`lake` 条目都是古代与现代水库，正文写明；
    海岸按海岸局 2024 的类型分：西南与东北岬湾（横向型）、西北与东南潟湖—沙坝—沙丘，河口分河道型（凯拉尼、马哈韦利）与盆地型（普塔勒姆、尼甘布、拜蒂克洛）；「三级准平原」只作简化框架写。
    **1 级来源**：统计局《Statistical Abstract》第 1 章（curl 可取）、中部高地世界遗产申报文本 2008（276 页 PDF，覆盖山地地貌 / 地质 / 保护沿革）、海岸保护与海岸资源管理局《海岸带管理计划 2024》、拉姆萨尔 RSIS 导出表（**邦德拉表头坐标录入错误**）、野生动物保护局 WordPress API；
    测绘局峰高与河长表取不到 —— 除最高峰外的峰高、全部河长只有 FAO（2 级，1980 年代）或维基；同一文件自相矛盾的数（国土面积 67,240 / 65,610 km²、海岸线 1,600 / 1,620 / 1,790 km、塞纳纳亚克坝高 34 / 43 m）正文不写或并列。
    「锡吉里耶是古火山岩浆栓」流传甚广但无地质依据，不写。
    **零争议红线本轮的特别项是西北与北部**：亚当桥 / 保克海峡 / 马纳尔岛与马纳尔湾一带、卡恰提武岛一律不收不写；**北部省整体不收**（英国外交部旅行建议仍提示排雷区与高度安全区）；
    东部省条目只写地貌、水文与海岸类型；族群、宗教评价（亚当峰只写「多个宗教的信徒前来朝圣」与朝圣季）、内战、军事与港口设施、马哈韦利 / 加尔奥耶工程移民定居、2022 年经济危机一律不写。
    `central-highlands-sri-lanka` 与越南 `central-highlands-tay-nguyen` 英文同名，`source` 注明。乌瓦盆地无 1 级地貌来源，以康提高原替代。
    旅游模式：1 概览 + 9 城（斯里贾亚瓦德纳普拉科特 / 科伦坡 / 康提 / 加勒 / 努沃勒埃利耶 / 埃拉 / 尼甘布 / 阿努拉德普勒 / 丹布勒）；**开工前读英国外交部旅行建议（2026-05-26）**：北部省、马纳尔不收，亭可马里（军港与军事设施密集、可写地标不足）、拜蒂克洛不收；尼甘布不列教堂类 POI。
    **首都措辞**：议会 1982 年迁到斯里贾亚瓦德纳普拉科特、科特 1985 年起被宣布为首都，科伦坡写「商业中心」，「最大城市」不写。
    **人口口径**：统计局 2024 年普查只发到县 / 分区秘书处 / 村官区，**没有市议会辖区人口**，城市一律写分区秘书处（科伦坡是两个 DS 合计，康提、努沃勒埃利耶的 DS 远大于市区要写明）；统计上只有市议会与城镇议会辖区算城镇（全国 17.5%），埃拉是乡议会辖区、统计上算乡村。
    机场：只有 CMB 有常规定期航班（在尼甘布东南约 6 km、离科伦坡约 28 km），锡吉里耶、科加拉等只有 Cinnamon Air 季节性小型航班，**除科伦坡外不填 airport**。
    铁路：2025-11 迪特瓦气旋后分段恢复，**努沃拉尔—巴杜勒（经埃拉）2026-06 复运、科伦坡—康提截至 2026-09 仍未复运**（当批核实改正），这类句子带「截至 2026 年 9 月」。
    惯例：G 型插座（PUCSL 1 级）、波耶日禁售酒、账单通常含服务费（只有 2–3 级来源，不写百分比）、自来水保守写。
    航线：`tfu-cmb` 国航 CA425 A320、`ckg-cmb` 重庆航空 OQ2393 A320neo（同号在科伦坡后续飞马累，比照 `pek-otp`）；**`pvg-cmb` 东航 MU6017 A330（上海机场集团官网 + AASL 周表，425 分钟）与 `can-cmb` 斯里兰卡航空 UL881 A320（白云机场官网 + AASL，325 分钟）2026-09-16 收** —— **UL880 / UL884 是科伦坡→广州的回程号**，广州出发是 UL881 / UL885，该线逐班换机、A330 亦有执飞；昆明 MU713（第二来源仍只有 FlightAware）2026-09-16 按 `flight-unknown` 收为 `kmg-cmb`，大兴首都航空 2026-09 周表无，不收。
  - **文莱**：`country: "brunei"`（东南亚次区域；陆地面积约 5,765 km²，地形按马耳他 / 卢森堡 / 新加坡的 **12 条**小国口径）；`COUNTRIES` 按拼音插在泰国与新加坡之间（Tàiguó < Wénlái < Xīnjiāpō）。
    **中文译名随中国外交部**：国名「文莱」（不是「布鲁内」）、文莱—摩拉区 / 马来奕区 / 都东区 / 淡布隆区、首都斯里巴加湾市；Brunei River / Bay 写「文莱河」「文莱湾」。**开工前读英国外交部旅行建议**（2026-03-19：无地区性提示，犯罪水平低）。
    地形主线：西块是冲积多沼泽的滨海平原 → 低丘 → 内陆又是沼泽（泥炭沼泽林约占国土 15.6%，林业局口径），东块从沼泽化平原经低丘升到淡布隆山地（山地林只在淡布隆区）；四大流域马来奕 > 都东 > 文莱河 > 淡布隆（FAO）；文莱湾内湾是全国红树林主体；默林本湖是「全国最大的天然湖」（旅游局口径，**必须带「天然」**，境内水库更大）。
    **本国 1 级来源普遍自相矛盾**（海岸线 161 / 168 / 130 km、泥炭沼泽 90,884 / 103,860 ha、国家公园 46,210 ha / 5 万 ha / 550 km²、塞利隆岛两页两个区属；统计年鉴把全国人口写成首都人口）—— 面积一律用林业局、人口用统计局 DEPS，冲突在注释里并列；河长除都东河 137 km（旅游局）外不写。
    **零争议红线本轮的特别项是「国土分两块」**：正文只写「东西两块」「湾内」，**一个邻国行政区名都不点**；国界划定与领土主张、湾内国界、界河、海域与离岸礁、南海岛礁一律不写；**最高点巴贡山在国界上，不作锚点**；多数锚点离陆地国界不足 10 km（国土窄，无法避开），正文统一不写「靠近边境」；王室、政治体制、宗教评价、族群比例、刑罚细节、油气产业评价与公司名不写；王宫、王室御用器物博物馆、陵墓、纪念塔不作 POI。
    旅游模式：1 概览 + 5 城镇（斯里巴加湾市 / 水村 / 都东 / 瓜拉马来奕（诗里亚并入，两镇合设一个市政委员会）/ 邦阿）。人口：区一级用 **DEPS 2025 年估算**（时点未注明），镇 / 乡 / 村只有 **2021 年普查附表 B**，两套不在一句里比；**首都市中心片区只住 314 人**、全国只有三个市政委员会、邦阿没有 —— howItWorks 主线。
    只有 BWN 有定期航班；小费只有 2019 年美国商务部来源，写「一般不需要」不写百分比；周五 12:00–14:00 全国关门、文莱元与新加坡元 1:1 互换、Grab / Uber 不可用（Dart）都有 1 级来源；时区、电压、行车方向无 1 级来源，不写。
    **航线 `pkx-bwn`**：文莱皇家航空 BI624 北京**大兴** PKX（不是首都 PEK），航司时刻表 + 中国民航网两源确认，A320neo 另有实飞记录；南宁、杭州暂不收 —— 2026-09-16 复查：**BI627 / BI639 是飞往南宁 / 杭州的回程号，中国出发是 BI628 / BI640**，号已由航司时刻表 + 文莱民航局航班信息两源确认，机型只有追踪站一源，2026-09-16 按 `flight-unknown` 收为 `nng-bwn` / `hgh-bwn`；深圳航空 2026-09-29 起才开航，10 月后复核。
  - 优先序（待做）：中国 Tier 2/3 城市继续扩张（批 11 已完成：衢州 / 连云港 / 日照 / 信阳 / 郴州 / 梧州 / 攀枝花 / 鞍山 / 通辽 / 佳木斯，中国旅游城市 146 → 156；**Tier 3 名镇批 1（2026-09-15）**：乌镇 / 周庄 / 黟县（宏村·西递）/ 阳朔 / 镇远 / 西江千户苗寨 / 九寨沟 / 元阳 / 建水 / 阆中，156 → 166 —— 名镇条目的 howItWorks 重点写「镇 / 景区 / 县」三层谁管什么，机场不在本县的不填 airport；**Tier 3 批 2（同日）**：同里 / 西塘 / 南浔 / 黄姚 / 婺源 / 永嘉 / 泸沽湖 / 武隆，166 → 174（湖州、上饶攻略同批删短重叠段；泸沽湖一个湖两省两个管理局、「泸沽湖镇」在四川一侧；婺源的国家级名村与收费景区不重合）；霞浦（来源薄、海域红线密）暂不收，稻城（2026 年景区被文旅部点名整改中）暂缓到整改结束；**Tier 3 批 3（2026-09-16）**：峨眉山 / 登封 / 千岛湖（淳安）/ 南岳（衡山），174 → 178 —— 四处的「谁管山」各不相同：峨眉山管委会是乐山市政府派出、2025 年起由峨眉山市代管；嵩管委是登封市政府派出、一个机构三块牌子；千岛湖没有管委会、县文旅局直管、央企合资经营；南岳区与省南岳管理局一套人马；同批删短乐山的峨眉山段、改掉郑州「少林寺在一小时高铁圈内」（登封没有客运铁路）；额济纳旗（结构写不全、红线近）暂缓；候选与做法见 `docs/city-expansion-progress-2026-09-05.md` §3.1）
  - **意大利**：`country: "italy"`（首个南欧国家）；只做本土 + 西西里 + 撒丁，海外无。
    阿尔卑斯跨境按「意大利与法国 / 瑞士交界」中性表述、不涉主权、不描述边界线；勃朗峰 / 马特洪峰 /
    罗莎峰如实标注在交界一带。南蒂罗尔 / 多洛米蒂的 `history` 只写地质 + 广泛记载的史前 + 一战山地战
    （简短事实）+ 拉迪恩语，不碰自治省政治；火山（维苏威 / 埃特纳 / 坎皮弗莱格瑞 / 斯特龙博利）按 INGV
    口径事实简述（庞贝 79 年、马莫拉达 2022 冰崩、坎皮弗莱格瑞 bradyseism 都如实）；卡尔索按「跨意大利
    与斯洛文尼亚」中性表述（"karst"一词的原产地）。褶皱山系 / 火山锥 / 高山 massif→`mountain_system`、
    破火山口→`basin`、中央高原式老陆块 & 喀斯特台地→`plateau`、软岩丘陵 & 低缓老地块→`hills`、
    前陆盆地大平原→`plain`、冰川→`valley`、峡谷 / 冰蚀圆谷→`gorge`、湖→`lake`、
    河→`river`、岛 / 火山岛群→`island`、峡湾岸 / 沙丘 / 白垩崖 / 潮滩 / 潟湖→`coast`。
    计划见 `docs/superpowers/plans/2026-08-30-world-terrain-phase-b-italy.md`。
  - **法国**：`country: "france"`；只做本土（métropole）与科西嘉，**海外省 / 海外领地不收**。
    跨境山脉（阿尔卑斯 / 比利牛斯）按「法国与意大利 / 西班牙交界」中性表述、不涉主权、不描述边界线；
    汝拉山脉瑞士条目已有、法国不再单列。褶皱山系 / 高山 massif / 火山锥群→`mountain_system`、
    中央高原→`plateau`、喀斯特台地（科斯 / 韦科尔）→`plateau`、低缓老地块（阿摩里卡）→`hills`、
    沉积盆地→`basin`、冰川→`valley`、峡谷 / 冰蚀圆谷→`gorge`、三角洲 / 沿海砂地→`plain`、
    峡湾岸 / 沙丘 / 白垩崖 / 潮滩→`coast`。科西嘉 `history` 只到广泛记载的史前 / 中世纪史
    （菲利托萨、热那亚塔、1768 并入法国），不碰现代自治 / 独立议题；韦科尔 / 塞文山的二战史保持简短事实。
    计划见 `docs/superpowers/plans/2026-08-30-world-terrain-phase-b-france.md`。
  - **挪威**：`country: "norway"`；地名挪威语为主、北萨米语并列（Finnmarksvidda / Finnmárkkoduottar）。
    只做挪威本土，**斯瓦尔巴 / 扬马延不收**。山系→`mountain_system`、山地高原/冰帽→`plateau`、
    冰川/冰蚀谷→`valley`、峡湾→`coast`、湖→`lake`、悬崖/急流→`gorge`、平原→`plain`。
    芬马克 / Sápmi 的 `history` 只写萨米驯鹿放牧这类广泛记载的土地利用作客观事实，不碰现代政治；
    斯堪的纳维亚山脉「与瑞典共有」中性表述、抬升机制按学界有争论表述。计划见
    `docs/superpowers/plans/2026-08-30-world-terrain-phase-b-norway.md`。
  - **瑞士**：`country: "switzerland"`；多语地名按 swisstopo 用法并列（DE/FR/IT）。
    山峰/山系→`mountain_system`（含孤峰马特洪峰、逆冲断层构造区）、冰川→`valley`、
    冰蚀谷→`valley`、湖→`lake`、瀑布/隘谷→`gorge`、中央高地→`hills`。博登湖主湖区无
    正式国界，按中性事实表述。
  - **冰岛**：`country: "iceland"`；无主权争议。火山锥→`mountain_system`、破火山口/地热盆地→`basin`、
    裂谷→`valley`、冰帽→`plateau`、外冲平原→`plain`、瀑布/峡谷→`gorge`。近年火山活动
    （雷克雅内斯/格林达维克）按 IMO 口径事实性简述。
  - **新西兰**：官方双语地名按 NZGB 用法并列（Aoraki / Mount Cook、Taranaki Maunga）；
    火山锥归 `mountain_system`、破火山口归 `basin`、峡湾归 `coast`、冰川归 `valley`。
  - **英国**：`country: "uk"`；威尔士语、盖尔语地名按官方用法并列；`history` 段只写自然史
    + 广泛记载的人类活动，不碰现代政治。北爱尔兰地形用中性地理名。
  - **日本：主权争议岛屿（南千岛/北方四岛、竹岛/独岛、尖阁/钓鱼岛）全部不收**；
    琉球条目只用无争议的南西诸岛主链。
  - 每个新国家 = 新分支，stack 在上一个国家的分支上（都改 `regions.ts` 该洲 terrainCount +
    共用 `terrain-content.{zh,en}.ts`）。新大洲则从 `design/world-terrain-expansion` 切。
  - 分批：每 commit 约 6–8 个注册表条目（按分类分组）+ 同 commit 写双语 6 板块，
    每次跑 `check-regions` + `check-terrain-camera`，浏览器抽查。
- **发布门槛**（2026-09-15 立，详见 `docs/expansion-playbook.md` §2.1）：判断收不收看内容能否达到准确性与中立性门槛，不对国家下评价——
  ① 英国外交部旅行建议劝阻前往全国 / 大部分地区或处于武装冲突的国家整体暂缓（如缅甸）；② 国家可发布但有地区级劝阻的，劝阻地区及其 20 km 内的城市、地形、航线不发布；**这条 20 km 是跟着「劝阻地区」走的，不是「所有国界」**——欧盟 / 申根内部那种没有劝阻的边界不适用（图尔奈离法国 9.5 km 照收，只是正文不写边境）；另按各国既定做法，白俄罗斯 / 俄罗斯 / 乌克兰这类正文本来就不能写的边境，其 20 km 内同样不收（2026-09-16 明确，此前逐案判过一轮）；
  ③ 执法机构通报的大规模跨境电信诈骗、人口贩运集中地区不发布旅行导向内容，相邻国家条目不写前往这些地区的口岸与跨境行程；④ 来源矛盾、变化过快、无法中立表述的暂缓。
  **开新国家、补新城市前先取 FCDO 页面**；2026-09-15 按此复核全库，撤下达沃 / 仙本那 / 合艾三城、阿波山 / 拉瑙湖 / 阿古桑沼泽 / 诗巴丹岛四条地形、马尼拉—达沃 / 宿务—达沃两条航线。
- **零争议红线**（本轮特别强调）：所有文字查该国官方来源（地质调查局 / 国家公园管理局 /
  官方地名机构 / UNESCO / 官方统计或旅游机构）；地名用中性通用名、有并用名以事实并列、
  不表述主权；`history` 段不碰现代政治 / 领土 / 宗教 / 族群评价；可能有争议 → 删或换中性表述。
- 广度优先：先把注册表 + 双语 6 板块写全；真实边界多边形、相机逐个精校之后再补。

## 内容目录（按国家，2026-09-14 拆分）

拆分前七个内容文件合计十万行（地形讲解中英各 1.9 万、城市攻略各 1.65 万、地形注册表 1.8 万……），
每加一个国家往中段插几百行，插错位置就把后面条目吞进上一条。现在：

- `lib/content/countries.ts` 的 `CONTENT_COUNTRIES` 决定各国在 `TERRAIN_REGISTRY` / `CITY_REGISTRY` 里的先后；
- `lib/content/<country>/` 最多 8 个文件：`registry.ts`（`TERRAINS`）、`terrain.zh.ts` / `terrain.en.ts`（`TERRAIN_ZH/EN`）、
  `cities.ts`（`CITIES`）、`travel.zh.ts` / `travel.en.ts`（`TRAVEL_ZH/EN`）、`pois.ts`（`POIS`）、`routes.ts`（`ROUTES`，
  **国内线放本国、国际线放非中国一侧的国家**）；
- `npm run gen:content` 生成 `lib/content/_generated/`（同步合并 + 按国家懒加载表），**不要手改**；
  `npm run check:content`（已在 `npm run check` 里）拦住：生成文件没重跑、条目放错国家、id 跨国重复；
- 原来的 `lib/terrain-content.{zh,en}.ts`、`lib/travel-content.{zh,en}.ts`、`lib/route-narration.ts`、
  `lib/travel-pois.ts`、`TERRAIN_REGISTRY`、`CITY_REGISTRY` 仍是合并导出，脚本与 import 不变；
  界面打开一篇讲解 / 攻略**只下载该国一份**；
- 按文本改内容的工具（`verify:apply-text`、`scripts/verify/mkfindings.py`、`gen:verify-status`、`sample:claims`）
  都按条目 id 找所在国家文件；新脚本要按文本读内容时用 `scripts/lib/content-files.ts`。
- ⚠️ `sample:claims` 的样本池顺序随拆分改变：seed=1..11 的结果以 `docs/quality-sampling.md` 的记录为准，拆分后从 seed=12 起。

## 浏览器测试（2026-09-14）

- `npm run test:e2e`（Playwright，`e2e/smoke.spec.ts`）：学习讲解按国家懒加载、中英切换正文跟着换、旅游攻略、
  航线镜头等解说开口。语音一律 mock，不依赖 Edge TTS。
- 本机默认用已安装的 Google Chrome；先 `npm run build`，或设 `E2E_BASE_URL=http://localhost:3000` 连正在跑的 dev server
  （**测试期间不要改 components/ 下的文件**，热更新会让地球重载、测试超时）。CI 里 `e2e` job 自动跑。
- 关键 UI 带 `data-testid`（`intro-enter` / `region-*` / `rail-toggle` / `rail-search` / `rail-result` / `reading-title` /
  `reading-article` / `mode-*` / `language-toggle` / `journey-*` / `route-card-*` / `flyover-name` …），改组件时保留。

## Camera 推导链路

```
TerrainEntry（registry: 锚点 landmark + bbox + axis + viewFrom）
  → computeTerrainCamera()  推导 { target, heading, pitch, range }
  → flyToTerrainAndWait(payload, { heading, pitch })
  → Cesium flyTo
```

- **锚点 landmark** = 该地形最标志性的地物（主峰/主湖/枢纽城市），相机对准这里。
- **heading/pitch/range** 全部由 bbox 规模 + axis 走向推导，不写死。
  仅 `viewFrom`（相机在锚点的哪一侧）、`viewScale`（取景放大系数）可按地形显式给定。
- **相机位置 target** = 从锚点沿 viewFrom 反方向按几何偏移，使锚点落在画面中部。
- 普通地形：`showKm` 封顶 120km。**大面积高原/大盆地/大平原/大沙漠**（青藏/内蒙古/塔里木/
  云贵/华北平原…）在 `terrain-registry.ts` 的 `WIDE_VIEW` 里给 `viewScale`（1.5–2.7），
  `showKm` 放宽到 320km、range 到 340km —— 让画面看出"一整片高地/盆地/平原"的地貌特征，
  而不是锚点周边一个局部景物（湖 / 城市）。
- 自检: `node --experimental-strip-types scripts/check-terrain-camera.ts`

调参常量集中在 `lib/terrain-camera.ts` 顶部（FRAME_HALF_ANGLE_DEG / PITCH_* /
SHOW_KM_MAX / RANGE_MAX / LANDMARK_SCREEN_FRAC），视觉取景需在真实浏览器中校准。

## 地形集 / 标签 / 区域高亮

- **`TERRAIN_REGISTRY`（当前 1344 个，见 §范围）= 单一真实源**：纯地貌图鉴，不含人文聚落
  （`settlement` 分类已于 2026-09-04 废弃移除，见 `docs/terrain-taxonomy.md` §8）。
  `TerrainCategory`：mountain_system / plateau / basin / plain / hills / desert / lake /
  river / valley / gorge / island / grassland / coast / inselberg。
  **选取标准与分类判据见 `docs/terrain-taxonomy.md`**（勿凭感觉加条目/改分类）。
  坐标逐个查权威来源，`source` 留痕。
- **侧边栏 + 地图标签都由注册表驱动**：`ExplorerApp.ALL_FEATURES` = `TERRAIN_REGISTRY.map`；
  `handleSelectById(id)` 统一入口（早期地形 JSON / core-features 有内容则讲解，否则占位）。
- 精确边界：`scripts/extract-ne-landforms.mjs` 从 `data/gis/ne_10m_geography_regions_polys`
  提取 42 个 → `public/data/gis/exports/{id}.geojson`。`CesiumMap.TERRAIN_RING_FILES` 列出这 42
  个 id，其余用 bbox 八边形。
- `CesiumOverlayLabels.tsx`：HTML 标签。
  - `projectToScreen` 用 `EllipsoidalOccluder` 剔除地球背面的点（否则缩小看地球会堆叠）。
  - zoom ≤ 3 不显示；importance→LOD 1:1，按 zoom 分级（大陆/国家/区域/POI）逐级展开。
  - `dynamicFontSize` 随 zoom 缩放，下限 0.8。字体 `LABEL_FONT_FAMILY`（通用系统字体栈）。
- **地形区域抬升高亮**（`CesiumMap.tsx`，每个地形三个实体）：
  - `pick`：贴地透明多边形，仅作拾取命中目标。
  - `lift`：`perPositionHeight` 多边形，顶面**跟随真实地形高程**（首次交互时
    `sampleTerrainMostDetailed` 采样并缓存），hover/focus 时整块 ease 抬升 3km / 5.5km。
  - `rim`：顶面亮边框 polyline（定高度、非 clampToGround）——**任意视角（含俯视）都能看清地块轮廓**。
  - 统一暖琥珀 `REGION_CSS`，hover / focus 只是强弱不同（α .20/.34，rim α .6/.95）。
  - **不要用 `polygon.outline`** —— 会懒加载 `createPolygonOutlineGeometry` worker，网络异常时崩溃。
  - `tickTerrainRegions` rAF 推进；配色/高度/透明度常量在 `CesiumMap.tsx` 顶部（`REGION_*`）。
- hover 走 `ScreenSpaceEventHandler` MOUSE_MOVE → **`scene.drillPick`**，在重叠命中的地块里
  取 `areaDeg2` 最小（最具体）的那个；LEFT_CLICK 同样 drillPick 取最小面积 → `onTerrainSelect(id)`
  → `ExplorerApp.handleMapTerrainSelect` 自动切大洲再讲解（跨洲直接点地图也能跳）。
  已知：`CesiumOverlayLabels` 仍只渲染当前大洲的文字标签，非当前洲的地形有 hover 抬升高亮但无标签，
  点一下才切过去。

## 语音播报

- **缓存是第一道防线，不是优化**：`lib/tts-cache.ts` 是缓存键/白名单/超时/合成的
  唯一实现，`app/api/tts/route.ts` 与 `scripts/warm-tts.ts` 共用。缓存键是
  `sha256(voice + " " + text)`，改键算法等于作废整个 `.tts-cache/`。
  预热 `npm run warm:tts`（可中断可续跑，支持 `--kinds/--langs/--ids/--limit`），
  覆盖率 `npm run check:tts`。
  ⚠️ **新增内容必须当批预热**（2026-09-17 立）：没命中缓存就要现场合成，中文冷合成实测约 19 秒，
  而 `section-narration` 连续 2 段失败就**整篇降级成浏览器 TTS** —— 那就是用户听到的「不自然的机械音」。
  所以「点开刚加的城市全是机械音、点开老城市是自然人声」不是音色配置问题，是**预热没跟上**。
  加完城市 / 地形 / 航线，跟「当批核实」一样跑一遍 `npm run warm:tts`（见 issue #307）。全站 23,802 段播报、约 4.9 GB、167 小时音频。
  **`.tts-cache/` 是本机 gitignore 目录，不进仓库**；换机器/换服务器重跑预热即可。
  **线上部署（Vercel）读不到本机缓存**：2026-09-14 起 `lib/tts-cache.ts` 支持远端只读缓存 ——
  把 `.tts-cache/` 上传到能按 URL 公开读的存储，设 `TTS_REMOTE_CACHE_URL`，本机未命中时去那里取（3 秒超时，取不到才现场合成）。
  全量缓存约 10 GB（实测平均每段约 0.4 MB），存储选型（R2 / S3 / Blob）要用户决定并提供账号。
- 播报文本清单由 `lib/tts-manifest.ts` 生成，一律调用客户端同一套函数
  （`resolveLesson`+`lessonSections` / `resolveTravelGuide`+`travelGuideToSections` /
  `getRouteNarration`），保证与线上请求逐字节一致 —— 不一致则预热白做。
- **语速常数分语言**（`lib/speech.ts` `CHARS_PER_SEC`：zh 4.44 / en **14.6** 字符每秒，
  2026-09-17 按 `check:tts` 的 120 段实测把 en 从 15.24 改过来 —— **这两个数以实测为准，偏差超 3% 就回来改**），
  从缓存音频的 word boundary 实测而来；`npm run check:tts` 会复算并比对。
  以前只有一个按中文校准的 4.5，英文被高估 3.4 倍，航线兜底节拍因此错得离谱。
- 降级策略是**连续失败计数**（`lib/section-narration.ts`，连续 2 段才整篇降级），
  不是「一段失败就永久降级」——后者会让一次瞬时超时把整篇变成机械音。
- `app/api/tts/route.ts`：Edge TTS，默认 `zh-CN-XiaoxiaoNeural`（最自然的中文女声），
  `PROSODY` 常量控制 rate/pitch（大幅放缓会有机械感，保持 -6% 左右）。
- 逐句高亮：Edge TTS 成功 → `startHighlightWithTiming`（word boundary 精确同步）；
  失败回退浏览器 TTS → `speakBrowserAndWait` 在 `utterance.onstart` 触发 `onPlaying`
  （不是等播完），`startHighlightSections` 按字数估时推进。
- **切句只许有一份，在 `lib/sentences.ts`**（2026-09-17 立，`npm run check:sentence-split` 强制）：
  - `splitSentences`：按句末标点切 —— **航线解说与航点锚定用它**（`route-anchors.data.ts` 的锚点表
    按它的句序生成，换了句数 `check:anchors` 会失败）；
  - `splitForHighlight`：**讲解 / 攻略的面板渲染与逐句高亮用它** —— 在句末标点之外，再按「朗读权重」
    （不是字数：中文里一个数字念出来是好几个音节）在从句标点 / 括号 / 引号处把过长的句子切开，
    每块目标约 5.5 秒，并且**在候选断点里挑最接近目标的那个**，不是遇到第一个超过目标的就切。
  - ⚠️ **面板渲染与高亮必须用同一个函数**。`StructuredLesson` 曾自带一份缺了小数点保护的实现，
    正文里只要出现「1300.2 米」面板就多切一句，**此后整篇全局句子索引错位**——
    全库 2,650 段城市攻略（20.5%）与 517 段地形讲解中招，表现为「语音念到后面了、高亮还停在前面」。
    这一类**类型能过、测试能过、两份实现各自都"对"，错的是它们不一致**，只能靠不许有第二份来守。
  - `npm run check:highlight-sync`：拿 `.tts-cache/` 里**真实的 word boundary** 跑生产用的时间映射，
    断言每块高亮的区间单调、不重叠、覆盖整段音频、且没有超过 13 秒的块（CI 无缓存时自动跳过）。
  **数字里的小数点不算句号**：「海拔 7508.9 米」裸按句号切会切出一句以「9 米，……」
  开头的残句 —— 高亮会跳、朗读在数字中间停顿、锚定还拿这半句去匹配地名。
- 英文解说是多段字符串拼接出来的，**上一段以句号结尾时下一段要以空格开头**，
  否则拼出「…of the flight.Easter Island lies…」这种粘连句（曾有 557 处）。

## 内容准确性：这个项目已经犯过哪些错、靠什么接住

**前提**：本项目 1352 处地形 × 6 板块 + 930 座城市 × 7 段 + 295 条航线 × 2 套解说
（中英双语）的文字，是**依据公开资料整理生成的，没有人逐条审核过**。
已经发现并修掉的事实错误不在少数，还没发现的一定也有。
**任何对外的文案都不要写"权威""人工审核""逐句核源""真实商业航班"这类断言。**

- **错误台账 [`docs/known-errors.md`](docs/known-errors.md)** —— 按类型记录犯过什么、
  根因、怎么修的、**现在有没有防线**。新发现一个错误：
  ① 先看它属于哪一类，把实例补进去；
  ② **归不进去 = 新类型**，新开一节并同时想清楚防线；
  ③ 开一个 GitHub issue（标签 `data-error`），正文第一行写它属于哪一类。
- **`npm run check:claims`** —— 扫「写法上就已经不可靠」的句子：人口数字缺年份、
  主观最高级、排名断言缺口径、英文拼接漏空格。存量很大，所以是**棘轮**：
  与 `docs/claims-baseline.json` 比**只许降不许升**，新写的内容加一句没年份的人口数
  就会失败。降下来之后 `npm run check:claims -- --update-baseline` 固化。
  **C1a（主观最高级）与 C1b（排名断言缺口径）已于 2026-09-12 双双清零、基线固化为 0** ——
  再写一句「最有名」「最重要的」「第二大」而不给口径，直接失败。清理过程与从中查出的真错见
  `docs/known-errors.md` 的两节；同日修掉规则里一处死代码（「同一城市月份之间比较」的豁免
  比的是 `m[0]`，只到「最好的」为止，「最好的时候 / 最好的季节」这一支从来没生效过）。
- **`npm run check:zhen`** —— 比**中英两侧同一字段、同单位的数字**
  （地形 6 板块 + 城市 7 段 + **航线两套解说**，全库约 6,500 对）。中英是分开写的，人核实时又是**按语言**读的，所以「两侧各挑了一个值」
  这种错只有机器看得见：`romsdalen` 的巨魔墙中文写 1000 米、英文写 1,100 m，
  而上一轮**已经查明有这两个值、还把冲突写进了注释**——不是没查，是查完没同步到两侧。
  判据刻意保守（两侧都得有该单位的数字才比、容差 6%、两侧都出现对方没有的值才报）。
  **中文侧的汉字数字也解析**（2026-09-11 补：「三千六百米」「一万一千五百公里」，「六七百米」按区间两端都记；「数十公里」「上百米」这种没有具体数的跳过）—— 第一版只认阿拉伯数字，航线解说的中文大量用汉字数字，梅塞塔中文「六七百米」对英文 600–800 m 就是这么漏过去、最后靠联网核实的子代理抓到的。
  ⚠️ **它每次都打印「每套内容各比了多少对」，任何一套比到 0 对直接判失败** ——
  这个脚本自己踩过三次「取不到值被当成没问题」（忘了 await 一个 async 函数、
  字段名写错成不存在的 `food`、航线的语言键是 `zh-CN`/`en-US` 而不是 `zh`/`en`），
  前两次都报了「全部通过」，第三次会被总数盖住（另外两套还有 5,921 对）。
  **任何检查都要能说出自己实际检查了多少个对象，而且要分到每一类上、不能只报总数。**
- **`npm run check:distance`** —— 把航线解说里的「全程约 X 公里 / about X km」拿去和`data/routes/*.json` 两端机场坐标算出的**大圆距离**比（不联网）。声称值比大圆还短 5% 以上一定错（飞机不可能比大圆飞得更短）；比大圆长出 15%（短于 1000 km 的航线放宽到 30%）也报；同段中英两个数相差 6% 以上也报。**中文侧的汉字数字（「一万一千五百公里」）它自己解析** —— `check:zhen` 只比阿拉伯数字，航线解说的中文又大量用汉字数字，这一整类原本在所有脚本视野外。2026-09-11 第一次跑，73 处里 14 条对不上，其中 11 条比大圆还短 —— 这类数**算得出来、不必查**，不该花一个联网子代理的工夫。有对不上时 exit 1。
- **`/verify-content`** —— 每批新增内容之后跑的联网核实流程
  （[`.claude/skills/verify-content/SKILL.md`](.claude/skills/verify-content/SKILL.md)），
  分批交给 `content-verifier` 子代理（[`.claude/agents/content-verifier.md`](.claude/agents/content-verifier.md)）
  逐条对权威来源。**拿不准就记 unknown，不要补一个看起来合理的答案** ——
  犯过的错里相当一部分正是"看起来很合理"。
- **`npm run verify:apply-text -- <findings.json>`** —— 机械地把 findings 里的替换打进正文
  与来源注释。打完会**自动回头搜**：把「这次改掉的那几个字」拿去搜五个内容文件 + 注册表，
  列出还留着的地方（只报告、不拦截 —— 命中不等于有错，判断归人）。
  这一步是从实际教训倒推出来的：2026-09-10 一天之内三次「改了查出问题的那一条、没回头搜别处」，
  三次都靠下一批的子代理才补上（`石炭纪` / `阿拉贡王室` / 捏造的「梅尔梅奥河谷」，
  最后那个**一共住在四个地方**）。
- **`npm run verify:report -- <findings.json>`** —— 把一轮核实结果**机械地**落地，
  不要手工开 issue（手做一定会重复开、忘记关）：当场修掉的自动评论 + 关闭对应 issue；
  查明有误但找不到替代值的自动开 issue（正文埋 `<!-- verify-key: -->` 去重，
  重复跑不会重复开、已关闭的会重开）；每轮在总 issue 下留进度评论并追加台账一行。
- **`scripts/verify/mkfindings.py`** —— findings.json 的生成器。`verify:apply-text` 是全有或全无的，
  所以三条前置条件在**生成时**就断言掉：find 在**该条目块内**正好出现一次、不跨 `" +` 片段拼接、
  替换里没有裸双引号（曾经一整批 40 条因为其中一条 find 在条目里出现两次而全部回滚）。
  它还带一个 `apply_direct()`：**纯写法改动**（降级最高级、统一措辞）不该开 issue、也不需要来源注释，
  用它批量改、同样全有或全无 —— C1a 那 283 处就是这么落地的。
- **`npm run check:issues`** —— 开着的 `data-error` issue × `check:claims` 的**当前**输出，
  找出「不再命中、可以回去关掉」的。立这个脚本是因为 2026-09-12 数了一遍：**47 条开着的 issue 里
  只有 20 条是真的还没解决**，另外那些分成三类 ——「早就修好/已核实为『确实没有更新的数』、
  只是没人回来关」「本来就是定时复核（末班日、开航日）」「类级别的总伞」。
  **「查明拿不到数」是结论、不是待办**：核完就该写进 `claims-stale-exempt.json` 并**关掉 issue**，
  否则它会被一遍遍重新读、重新排进下一轮清单 ——「错永远修不完」的感觉有一半来自这里。
  ⚠️ 它**只对 `check:claims` 真的实现了的错型表态**：C6-j 归 `check:pending`、C6-c / C3-b 没有脚本，
  那些「不再命中」不是解决了，是**防线看不见它**（仙台与大阪那两条就是这么一直开着的）。
- **`npm run sample:claims` + [`docs/quality-sampling.md`](docs/quality-sampling.md)** ——
  **唯一能回答「用户随手翻开一句，它错的概率是多少」的东西。**
  此前所有进度都是覆盖率口径，而覆盖率有两层虚高：① **一轮核实只要在某条目里改了一句，
  整条就算「核过」**，而同条目另外五段没人看过 —— 按字段粒度重算（`npm run gen:verify-status`）
  是 **2,199 / 11,395 ≈ 19%**，不是按条目算的 89%；② **「有人核过」不等于「是对的」**。
  台账里累计裁决 2,948 条、其中 1,935 条有误（66%）**也不能当错误率用** —— 那是脚本挑出来的
  可疑句，有偏样本。正确率只能靠**均匀随机抽样**：`sample:claims` 按固定种子抽一份可复算的样本，
  交给 `content-verifier` 逐句核，结果记进 `quality-sampling.md`。
  **第 1 次（seed=1，n=30）：24 ok / 2 wrong / 0 unknown / 4 n/a，错误率约 7.7%**，
  外推全库还有 500–900 处错误。⚠️ **不要挑样本** —— 抽到难核的照样要核照样要记，
  一旦开始换样本，这个数就和覆盖率一样没有意义了。每推进一大批内容之后换种子重抽一次，
  **这是唯一能说明「核实工作到底有没有让内容变好」的指标**。
- **`components/VerificationNote.tsx`（阅读面板底部的核实状态一行）** —— 诚实声明此前只写在
  README 里，而**读内容的人不看 README**。现在每篇底部都如实写「本篇 N 段中 M 段有核实留痕
  （最近 X 日），其余依据公开资料整理」+「有留痕不等于每句都对」。数据由
  `npm run gen:verify-status` 从 `sources:` 注释生成（`lib/verification-status.data.ts`，
  不要手改）。**目标不是把错全改完（做不到），是让读者能自己判断该给多少信任** ——
  一个人事先知道这里没逐条核过，撞见一处错不会推翻他对全部内容的信任；
  如果他以为这里是权威的，一处错就够了。
- **[`docs/verification-next-steps.md`](docs/verification-next-steps.md)** —— **新会话从这里开始**：
  当前各项计数、56 个开着的 issue 分成哪三堆、14 个卡在「公报是图片 / 证书失效 / 反爬」上的怎么换手段、
  优先序、以及要用户点头才能做的两件事（下载 OurAirports 机场数据、下载图片公报做 OCR）。
- **写新内容时的硬性习惯**（这几条是从实际犯过的错倒推出来的，多数已由脚本强制）：
  ① 人口/面积一类数字**必须带年份与统计口径**，并在条目上方写 `// <字段> sources:` 注释；
     正文写「**年份 + 口径 + 数字（机构简称）**」，完整公报名只进注释 —— 正文会被 TTS
     念出来，把《XX年国民经济和社会发展统计公报》整名念一遍不可接受。
  ①-a **口径先于数字**：先确认这个口径在该国官方统计里**存不存在**。「都会区人口」
     「市区人口」「中心城区人口」在中国都不是官方称谓，公报只有「全市常住人口」与
     「城镇人口」两档 —— 口径是编的，数字再精确也没意义（known-errors C6-c）。
     **这一条现在有脚本了**：`check:claims` 的 `C6k` 按条目所属国家查一张「这个国家没有的
     口径」表（目前覆盖中国与丹麦，见 `scripts/check-claims.ts` 的 `FAKE_CALIBER`）。
     表里**只写已经核实过的国家**——没核过的凭印象往里加，等于用一条猜测去拦另一条猜测。
     判据必须按国家：「都会区」在美国 MSA、日本都市圏都有官方口径，在中国与丹麦则没有。
  ①-b **必须是最新一期**：N 年的年度数据一般在 N+1 年上半年发布，所以在 N+1 年的下半年
     写 N−1 年的数就是落后一期。`npm run check:claims` 的 `C6d` 会把「统计时点早于
     当前年 − 1」的句子逐条报出来（普查除外）。核实时要一并问清该机构的**发布月份**，
     记进注释，好让下一轮知道什么时候回来刷新（known-errors C6-d）。
     **各机构的发布月份汇总在 [`docs/data-refresh-calendar.md`](docs/data-refresh-calendar.md)**；
     新增一个国家时顺手把它的统计机构补进那张表——发布节奏是核实过程的副产品，
     当时不记，下次还得再查一遍。
     **反过来，「年份旧」不等于「过期」**：有些地方就是没有更新的数（该级别公报不含人口、
     那年起只发户籍、那一版删了人口章节、公报是纯图片取不到数）。这类核实过之后要写进
     [`docs/claims-stale-exempt.json`](docs/claims-stale-exempt.json)，
     `check:claims` 会把它们从 C6d 计数里摘出来单列，并在到期时提示回来复核。
     **不写进去的话，下一个人会为了让计数下降而硬填一个没核到的数字。**
  ①-c **不要拿国际机构的推算冒充本国官方数字**：UN World Population Prospects /
     World Bank 是模型推算，本国统计机构是登记或普查，可以差几百万（known-errors C6-b）。
  ② 最高级与排名要么给限定语（「之一」「按行政区划面积计」），要么不写；
  ③ 机场名、行政区名这类专名会更名（乌鲁木齐地窝堡→天山 2025-03；呼和浩特白塔→盛乐**原定 2025-11-27 转场但未如期执行，截至 2026-09 仍是白塔在运营、无新的官方日期** —— 本库曾提前把它写成已迁建，**尚未发生的更名同样不能提前写**），
     写入时记下核实日期；
  ④ 英文正文由多段字符串拼接时，上一段以句号结尾则下一段以空格开头；
  ⑤ **价格必须带年份**，否则改写成不会过期的表述（车程、相对贵贱）—— 读者会拿价格当
     预算依据（`C6e`；维也纳年票 365 欧元十四年没变，2026 年 1 月涨到 467 欧元）；
  ⑥ **签证天数一律不写死**，只说以官方最新公布为准（`C6f`，此前只对中国有这条口径，
     现已由脚本对所有国家强制）。
  ⑥-a **本项目有三套平行的内容系统，改一处要想到另外两处**：
     地形讲解（`terrain-content.*`）、城市攻略（`travel-content.*`）、
     **航线解说（`route-narration.ts`，280 条 × 两套 × 中英）**。
     它们讲的是同一批地方，却是分开写的 —— 而分批核实**十几轮下来只覆盖了前两套**（**2026-09-11 补上**：280 条航线解说联网核完一遍，70 条改了 94 处，大多数是「地形 / 城市条目或航线数据早已改对、解说没跟上」—— 见 `docs/known-errors.md`；#264 已关闭）。
     实证：阿斯塔纳改名年份、苏梅岛「泰国湾第二大岛」（**台账里 `C1-b-2` 立类用的正是这个例子，
     城市条目当时就改对了**）、戈壁被叫成「沙漠」（而 `gobi` 条目有两整段在教「戈壁不是沙漠」）
     —— 三处都是条目已改对、航线解说没跟上。
     **这一步现在有脚本了**：`npm run check:rvt`（`scripts/check-route-vs-terrain.ts`）把两边
     点到同一个地形／锚点、又都带最高级的句子摆在一起，不联网。2026-09-11 第一次跑，
     168 组配对里捞出 5 处真错，**全是「地形条目改对了、航线解说没跟上」**（格伦维「最大」
     实为第二大、利亚伊马「超过50次」、凡尔登「欧洲最大的峡谷」、博恩霍尔姆丢了「之一」、
     瓦特纳「占冰岛八分之一」实为约 7%）。
     **它不是棘轮也不是门禁，永远 exit 0** —— 判对错要人看，标 ⚑ 的是值得先看的。
     ⚠️ 改完 `route-narration.ts` 必须重跑 `npm run gen:anchors`。
  ⑦ **会过期的量不止人口**（known-errors `C6-j`）：湖泊面积、冰川面积、海岸线、沙漠边界
     都在被某个过程持续改变，而 `C6`/`C6d` 的正则只匹配人口，**没有任何脚本盯着这些量**。
     写这类数字必须带监测时点，并在 `sources` 注释里写明下一轮回来刷新；能用区间就用区间
     （鄱阳湖「丰水期可达 3000 多平方公里、枯水期只有几百平方公里」比补一个年份还稳）。
     两说并存、都只有 3 级来源时**并列两说 + 写明尚无权威口径**，不要押注一个值
     （`qilian` 团结峰高程、`yinshan` 长度、`lake-trichonida` 面积都是这个写法）。
  ⑦-a **「会过期的状态」这一半现在有脚本了**：`npm run check:pending`
     （`scripts/check-pending.ts`）把三套内容里「在建 / 计划 X 年通车 / 停航，复航时间以公告为准 /
     20XX 年公报尚未发布」这类句子逐条列出来，**句中承诺的年份已经到了或过去了的标 ⚑**。
     它不是门禁、永远 exit 0（存量大半是合法的「确实还没建成」），解决的是
     「这些句子过去没有任何清单、过期与否只能靠人撞见」—— 呼和浩特机场被提前写成已迁建、
     牡丹江停航一年多没人回来看，都是这么来的。写这类句子时**自带时点**
     （「截至 2026 年 9 月仍未复航」）比写「以公告为准」更经得起放置。
  ⑧ **口径必须写死在句子里，不能只写一个笼统的词**（2026-09-12 立，known-errors「一个词可以在
     同一个库里指四种东西」那节）。「城区」在本库里同时指过：市辖区合计（哈尔滨 698 万，对的）、
     城镇人口、「环翠区 + 其代管的三个开发区」（威海，数字是真的、标签贴错）、城关区一个区（拉萨）。
     **数字对不对是一回事，标签指什么是另一回事。**写「九个市辖区合计约 698 万」，不要写「城区约 698 万」。
     旁证：出问题的 11 个条目**没有一个在 `sources` 注释里留下过这个数的出处**，
     而留了出处（写着「2020 年普查口径」）的 17 个抽查未发现问题 ——
     **留痕不是形式，是下一轮能不能判断的前提。**
  ⑨ **口径词要离数字足够近，否则脚本看不见**（2026-09-12 立）。「广域仙台都市圈**（宫城县指定，
     14 市町村）**约 150 万人」里口径词与数字隔了 16 个字，`C6d` 的窗口只有 8 个，于是这句
     **停在 2015 年国势调查**的话从来没被报过、对应的 issue 就那么一直开着。
     写的时候把口径挪到数字旁边（「2025 年国势调查速报的**区部**人口约 995.3 万」），
     **让脚本看得见和让读者看得见是同一件事。**
  ⑩ **外部数据集是筛子，不是来源**（2026-09-12 立）。用 CC0 的《县级以上行政区划代码》比对 106 个
     中国城市条目，100 个对得上、捞出 1 个真错；当轮还把重庆「26 个区」的命中判成数据集漏条目 ——
     **2026-09-15 抽样查出判反了**：2025-11 撤销江北区、渝北区设立两江新区（重庆市政府公告），数据集是对的，
     正文没跟上，当轮写下的「民政局公告确认仍是 26 区」找不到出处。**判数据集错之前同样要回一手公告。**
     每一条命中都要回一手（国务院 / 省政府 / 民政部公告）确认之后才动正文。
     同理适用于 OurAirports：它的错法是**滞后**（新机场没收录），区划那份的错法是**漏条目**，
     而漏条目在「数数」这种用法里会直接变成假阳性。

  ⑪ **「本国惯例」类句子换国家必须重新查，不能从邻国复制，也不许凭印象写一个百分比**（2026-09-13 立，抽样 seed=7 抓到；2026-09-16 补第二个实例：克罗地亚 9 个条目的「小费约 10%」在本国官方来源里查不到任何比例，改成「出于自愿、纳入财政化申报」不写数字）。
     小费与服务费、插头与电压、自来水能不能直饮、付款方式 —— 这几类句子是按国家写的模板，
     **抄邻国等于赌两国惯例一样**。波兰 13 个条目都写着「餐厅账单通常已含服务费，习惯上再给
     5%-10% 小费」，而波兰账单通常**不含**服务费；隔壁捷克、德国写的都是「通常不含」，只有波兰反了。
     这一类错误**任何脚本都抓不到**：`check:claims` 看写法、`check:zhen` 比中英数字，
     而这 13 处中英完全对称、写法也没问题，两个脚本一声没响 —— 只有随机抽样会撞上。
     代价也不对称：**写错一次被套 13 遍，抽到的概率乘 13，修的成本却和修一处一样**。

  ⑫ **不写机场的具体航点列表**（2026-09-13 立，`check:claims` 的 `C6l` 基线 0 强制）。
     「有北京、上海、成都等航线」「has flights to Sydney, Melbourne and Brisbane」随航季增删：交通状态回扫抽 14 条，
     **5 条已有航点停飞或其实是经停（36%）**——日喀则无重庆、林芝无拉萨直飞、临沂与腾冲的北京航线已停、长白山的「广州」经停大连。
     全库 76 处一次改成「有定期航班 / 有国内与国际定期航班」。**同一回扫里公交 / 电车 / 地铁线路号 30 条只错 2 条**
     （图卢兹机场 T2 电车 2023-06 停运、布莱顿 13 路只在节礼日开），线路号可以写，但要带核实日期；
     `npm run check:pending -- --transit` 列出全库这类句子，按国家复核时一并看。

  ⑬ **「没有机场」必须带限定：写「没有定期航班的机场 / no airport with scheduled flights」**，中国条目写「没有民用机场」
     （2026-09-14 立，`check:claims` 的 `C6n` 基线 0 强制）。读者要知道的是「能不能飞到这里」，而绝大多数「没有机场」的地方
     其实都有不飞定期航班的小机场或军用机场：爱沙尼亚当批核实抓到拉克韦雷、奥泰佩两处，拿 OurAirports 回扫全库 114 处，
     剑桥 CBG、布莱顿 ESH、科尔马、鲁昂、锡耶纳都有机场；**科尔多瓦连「没有商业机场」都不成立**（Aena 列出两条定期航线）。
     写「没有定期航班」之前先跑 `npm run check:airports`，第四段会列出「说了没有、20 km 内却标着有定期航班」的条目。
     **方位词同理**：写 layout 段的「X 在城北 / 往东是 X / X 在 Y 西北」之后跑 `npm run check:layout-bearings`（按 POI 与城市坐标算方位角，差出 67.5° 报出；带距离的句式归 `check:bearings`），2026-09-15/16 三次写反的方位都是这一类。
     **反过来，OurAirports 标有定期航班也不等于真有**（2026-09-15 立）：宁蒗泸沽湖 2025 年全年起降 96 架次、重庆仙女山 208 架次（民航局吞吐量排名），
     中国小机场写「有定期航班」前先查民航局《全国民用运输机场吞吐量排名》的起降架次，太少的写「航班很少，出发前查询」、不填 airport。

  ⑦-b **排他性断言（唯一 / 首座 / 首个）不要指望库内交叉比对**。2026-09-12 做过一个
     按「对象类别 + 范围」配对的扫描器并拿历史错误回测：阿拉拉特、格伦维那两条**一条都没抓到**，
     因为证伪依据（苏芬火山的冰帽、威克洛的面积）**根本不在正文里、只在注释或站外**。
     这一类的防线是核实流程里的人工规矩 + 把依据写进 `sources` 注释，不是脚本。
     详见 `docs/known-errors.md`「名次冲突原型」那一节后面的记录。

## 航线的航班信息：核实留痕（2026-09-07）

**问题**：城市与地形注册表一直强制 `source`，航线是唯一没有的一档 —— 于是
「这条航线是真的吗、机型对不对」没法离线回答。实测确有失效案例：上海—奥克兰
MU779 的机型原记 787-9、实际是 777-300ER；上海—东京 MU523 实际飞成田不是羽田。

**做法**：`FlightRoute.source = { ref, checkedOn, status?, note? }`
（`types/route.ts`）。`status` 缺省视为 `verified`；`"wrong"` = 查过了、查明记录
有误，但还没找到可靠的替代航班号。`"flight-unknown"`（2026-09-13 新增）= **机场对与承运人属实、但航班号/机型/班期拿不到
可核实的来源** —— 这类航线的 `flight` 字段可以整个缺省，`check:routes` 反过来会拦住
「声明了 flight-unknown 却又填着 flight」。立这个状态的理由是：斯洛伐克的
布拉迪斯拉发—科希策线确实存在（2025 年 11 月由 Wizz Air 复航），此前只有两个选择 ——
要么不收这条真实存在的航线，要么填一个「看起来合理」的航班号，**两个都不对**。
它与 `"wrong"` 的区别在于下一轮的入手点不同：前者要找替代号，后者要找时刻表。

**没核实（或核实为 wrong）的航线不显示任何航班信息**：`lib/routes.ts`
`isFlightVerified()` / `verifiedFlight()` 是唯一入口，`JourneyBar` 的航班号、
`routeSearchText` 的搜索索引都走它；`npm run check:routes` 会硬性拦截「未核实航线
的解说里点名了机型或航班号」。2026-09-11 起它还逐句比对「是 X 运营 / 由 X 执飞 / operated by X」里的 X 与该航线的 `flight.airline`（对不上就失败），并把支线机型（ATR / Dash 8 / Embraer / Saab / CRJ）纳入机型规则 —— 原有的航司报告探针取自数据里出现过的航司名，看不见解说自己写错的航司（蒙古 5 条「航蒙航空」），而机型规则只认空客 / 波音 / C919；两处盲区一次补出 13 条航线的承运人或机型与数据不符。**航线照常可飞、地理解说照常播** —— 地理是查得实的，
时刻表才是会变的那部分。

**现状（2026-09-13 更新）**：281 条航线里 **275 条显示航班信息、6 条不显示**
（5 条 `status: "wrong"` + 1 条 `status: "flight-unknown"`）。
2026-09-07 全库核完一遍时是 280 条 = 91 属实 + 163 有误已改正 + 26 有误且找不到可靠替代；
2026-09-12 把那 26 条里的 **19 条补了回来**（含一条起降机场对本身不成立、整条改到大兴的
`pek-wus` → `pkx-wus`），另一条广州—拉萨查明**全部航班都经停**、按既定口径整条移除（280 → 279）。
剩下 5 条各自的卡点写在 `data/routes/*.json` 的 `source.note` 里。
**189 条至少有一处错，占 68%**；冰岛 / 挪威 / 法国 / 意大利 / 西班牙 / 葡萄牙 / 丹麦 /
蒙古八国的国内线错误率是 100%。另有 16 条现实中没有不经停航班的航线已整条移除
（295 → 279；不用「不显示航班号」把一条飞不了的线留在库里）。
**新增 / 修一条航线时的硬性顺序**（2026-09-12 立，从这一轮的实际教训倒推）：
**先问「这个城市对有没有不经停直飞」，再问「航班号是多少」** —— 顺序反了会白查一整轮。
`pek-wus` 就是这么来的：连查两轮航班号都查不到，第三轮才发现**北京首都 PEK 与武夷山之间
根本没有直飞**，真正存在的是**大兴 PKX**—武夷山。这不是「找不到号」，是**起降机场对本身不成立**，
整条航线（id / 航点 / 解说里的机场名）都得改。配套的三条：
- **机场三字码必须精确对上**：PEK ≠ PKX、SHA ≠ PVG、CTU ≠ TFU。同城两场是本库最常踩的坑。
- **经停的同号航班不算直飞**（广州—拉萨全部经停重庆或昆明，就如实记「无不经停航班」）。
- **两个互不相同的来源指向同一个号才算确认**；一个号在两轮里被查成两个不同城市对时
  （`sha-lya` / `pvg-dat`）**不选边**，维持不显示 —— 航班追踪站与检索摘要都会把号配错城市对。

**新写航线的默认假设仍是「未经核实即不可信」** —— 68% 是在有意识地查资料之后写出来的
数字，不是随手编的结果。核实流程见 `.claude/skills/verify-content/SKILL.md`，
错误分类与防线见 `docs/known-errors.md`，逐轮记录见 `docs/verification-ledger.md`。

## 航线飞行

- **运动模型全部在 `lib/cesium/route-flight.ts`（纯函数，不依赖 Cesium/React）**，
  自检 `npm run check:flight` 对全部 295 条航线按 60 Hz 跑断言：进度映射单调、
  地速/取景高度比 ≤0.6、加速度、转向速率 ≤25 度/秒、飞行时长 ≥ 解说时长。
  **改这块必须先跑这个脚本**，别靠肉眼看。
- 时长**按距离分级**（90–180s，必要时延到 420s），且永远 ≥ 解说时长；
  取景高度跟随**峰值**地速（高度 = 峰值 × 2.5 秒），保证「每秒扫过多少个取景高度」
  在长短航线上恒定（**2026-09-17 复核：国内 / 国际都是 0.57，红线 0.6** —— 原先文档写的 0.44 / 0.43 是
  「取景高度加地速地板」那次改动之前的旧数，已过期；0.57 离红线只剩一成余量，再加长航线要盯着这个数）。航线距离相差 108 倍，
  用同一个时长必然让长航线糊掉。
- **未锚定的航线还要压「干飞」**：时长取「按距离」与「解说 + 75 秒」的较小值
  （`MAX_SILENT_TAIL_SEC`）。旅游模式的解说讲签证、季节、饮食，只有一分钟上下，
  常规上限原先是 300 秒，于是北京—巴黎这类航线播完之后还有两分多钟没有声音、
  画面匀速平移 —— 用户读到的是「点了一下之后卡住了」。`npm run check:flight` 因此
  卡的是**解说播完后还要干飞多久**（上限 105 秒，含限速拉长的余量），不是绝对时长：
  北京—纽约学习模式解说本身就有四分钟，镜头陪着飞四分钟没问题。
- 解说长度上限也随之改成按秒算（`npm run check:routes`，中英各自 ≤300 秒），
  不再是写死的「中文 900 字」—— 那是镜头时长还是常量时定的规矩。
- 进度是**帧率驱动**，单帧推进封顶 100ms。**不要改回跟随 `audio.currentTime`**——
  媒体元素的播放位置是台阶式更新的，高地速下每级台阶就是几公里跳跃，那是「抖」的来源。
- 位置曲线用以弧长为节点的 Barry-Goldman（弦长参数化）Catmull-Rom，航点处 C1 连续；
  停留平台两侧用梯形速度曲线；朝向按固定行进时间窗滑动平均（否则密集航点会甩头几百度/秒）。
- **解说锚定（学习模式）**：`lib/route-anchors.data.ts` 是逐句锚点表，
  `npm run gen:anchors` 生成、可人工编辑（`source: "auto"` 会被重跑覆盖，
  `"manual"` 永远保留）。有锚点表时镜头按解说排：讲到某个航点，镜头就在那里。
  **改了 `route-narration.ts` 的解说文字，必须重跑 `npm run gen:anchors`**，
  否则句数与锚点表对不上，`npm run check:anchors` 会报错。
  当前 560 篇（280 条 × 中英）里 530 篇已锚定，**待人工补 0 篇**；另 30 篇所属航线本来就没有
  地形/地标航点，无从锚定也不需要。
  旅游模式不做锚定：那边的解说讲的是高原反应、进藏许可、季节、目的地城市，
  不描述沿途地貌，66%–82% 的篇目一个航点都没提到。
- **匹配规则都是踩出来的**（`lib/route-anchors.ts`，改之前先读那里的注释）：
  首句是预告不是位置（「终点是赤道附近的爪哇岛」）；收尾句往回跳是回望式总结
  （「这趟航班穿越了从戈壁到欧亚大草原」）；航点名要去掉括注（「戈壁（蒙古）」）、
  拆开并列名（「Lake Lucerne / Vierwaldstättersee」）、去掉通名后缀
  （「Gobi Desert」→「Gobi」）；英文按词匹配，否则「Easter」会命中「eastern」；
  一句里多个航点取**句中位置**最靠前的、同位置取更长的；核心词太常见的要停用
  （「North Sea」→「North」、「长江三角洲」→「长江」）。原本计划人工补写的
  182 篇，最后是靠修这些规则 + 改 8 处解说文字清零的，没有手写一张锚点表。
- **锚定必然带限速**：解说会在两句之间跨越几千公里，照排镜头地速要到 460 km/s，
  画面必糊。速度卡在「不锚定时本来就会达到的峰值」以内，文字跳得急时镜头会落后，
  `npm run check:anchors` 会报出每篇的镜头滞后（中位 6 km、p90 382 km，
  12 篇长程超 800 km；起点是中位 319 km、最大 8344 km）。
  滞后只量「头一次点到某个航点」的那一句 —— 沿用上一句锚点的续写句位置本来就该
  由插值决定，把「飞机降落在戴高乐机场」算成超前一千八百公里是量错了对象。
- **解说要按各段航程配额写**：镜头有速度上限，一段航程占全程 35% 却只分到 15% 的
  篇幅，镜头就必然落后一两千公里，补锚点没用。二十四条洲际航线（pek-jfk / pek-cdg /
  pek-lhr / pek-bru / pkx-ams / pvg-arn / pek-prg / pek-ber / pek-cph / pek-waw /
  pvg-akl / pek-ist / pkx-hel / pek-cgk / pvg-zrh / pek-yyz / can-syd / pvg-dub /
  pvg-vie / pvg-mad / pkx-lis / pek-fco / pek-osl / pvg-ath）已按这个口径重写：
  每个航点分到的字数 ∝ 它到下一个航点的距离，总长 ≈ 该航线的镜头时长 × 语速。
- `CesiumMap.flyRoute` 把镜头摆到起点机场上空 → 开始播 `route-narration.ts` 里这条
  航线专属的连贯解说 → 同时镜头沿航点飞完。
- **不再逐个航点念地形讲解**。`onFlyoverWaypoint` 只把当前飞越的地形名同步到右侧面板，
  面板显示整条解说 + 逐句高亮。
- `RouteFlyCallbacks`：`onNarrate`（返回 Promise，播解说）/ `onFlyoverWaypoint` / `onComplete` / `onCancelled`。
- 弃用：`flyLeg` / `flyToRouteOverview` / `legDurationSec`；`FlightRoute` 的
  `minLegDurationSec` 等逐段计时字段改为可选、不再读取。

## 禁止事项

以下内容不再维护:

- `cameraGeometry`（china-core-features / china-northwest-features 中的字段，已停止读取）
- `lib/foi-registry.ts` / `lib/auto-camera.ts`（已删除，2026-09 技术债清理轮确认零引用后移除）
- Feature hoverGeometry/RidgeCorridor 渲染、`data/gis/exports/*.geojson` 边界线
  （geojson 现只用于地形区域抬升体的形状，不再画常驻边界）
- 手工 `target` / `heading` / `pitch` / `range`
- 逐个修补秦岭、大兴安岭等 Camera 参数
- 分散在 data/*.json、cameraGeometry、terrain-label-registry 的重复坐标
  → 统一到 `lib/terrain-registry.ts`
- 继续研究 GMBA / HydroBASINS 数据源（已验证完成）

## 代码分析工具

本项目使用 `codeatlas` 进行代码分析和架构理解。

```bash
# 安装（一次性）
cd ~/Github_Projects/codeatlas && pip install -e . --break-system-packages

# 索引项目
codeatlas index . --verbose

# 常用命令
codeatlas stats                          # 项目统计
codeatlas symbols <name>                 # 查找符号
codeatlas callers <function>             # 谁调用了这个函数
codeatlas callees <function>             # 这个函数调用了谁
codeatlas chain <function> --depth 3     # 调用链
codeatlas deps <file>                    # 文件依赖
codeatlas graph <name> --type calls      # Mermaid 调用图
```

详细说明见 `~/Github_Projects/codeatlas/README.md`

## 架构文档

- `docs/architecture-metrics.md` — 项目健康度指标、模块耦合度、Architecture Smells（codeatlas 自动生成）
- CesiumMap 六维分析 → `codeatlas file components/CesiumMap.tsx`
- 完整架构报告 → `codeatlas stats && codeatlas deps components/ExplorerApp.tsx --depth 2`

编排路线参考：[[architecture-intelligence-direction]]

---
## 首屏相机飞行约束（反复出现过的回归，务必遵守）

**相机的每一次自动飞行，必须能追溯到本次会话里一个真实发生的用户动作**
（点「开始探索」、点顶栏大洲下拉、点次区域、点地形/城市……），绝不能由
"地图初始化好了"或"localStorage 里记着上次停留在哪"这类跟*当次访问*无关的
时机来触发。恢复 `activeRegion`这个**状态**（让侧栏/标签显示对的大洲）没问题，
但恢复状态绝不能连带触发一次相机飞行。

**已经踩过的坑，不要重犯**：`IntroOverlay`（旅游模式欢迎卡）对"已看过引导页"的
用户会静默跳过，不显示卡片、直接把 `showIntro` 置为 `false`——这个跳过发生在
Cesium 完成初始化*之前*。任何在"Cesium ready"或"showIntro 变 false"这类时机点上
挂钩自动飞行的代码，都会把"用户以前看过一次引导页"误判成"用户这次访问已经
确认过了"，造成"没等用户选择，首屏加载页面就自动飞到上次的大洲"这个 bug——
这个 bug 已经复现过不止一次。详细背景见 `components/ExplorerApp.tsx` 里
（原 `handleMapReady` 所在位置）的说明注释。

---
## 开发规范

- 不要在未完成审计前修改 Camera 逻辑
- 不要在未验证数据源前导入地形数据
- 每完成一个 Phase，先更新文档再提交代码
- 业务代码和文档分开提交
- 修改 codeatlas 后先跑 `cd ~/Github_Projects/codeatlas && pytest -q && ruff check codeatlas/` 再 push
